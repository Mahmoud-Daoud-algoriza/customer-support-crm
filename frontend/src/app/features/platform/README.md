# features/platform — Story 01 scaffolding only

`HealthCheckComponent` is the temporary landing screen that proves the SPA reaches the API
(`.squad/plans/platform-foundation/01-story-solution-skeleton.md`, task 11).

**Story 02 replaces it with the role redirect** described in `docs/ui-design.md` §2
(`/` → Customer → `/portal`, staff → `/workspace`). When it does, this folder goes with it — it is
not one of the four areas fixed by `docs/architecture.md` §2.2.
