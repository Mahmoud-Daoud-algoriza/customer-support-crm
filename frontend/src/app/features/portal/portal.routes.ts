import { Routes } from '@angular/router';

/**
 * The Customer area (docs/ui-design.md §2): my requests, submit a request, request detail, help.
 * Lazily loaded behind a Customer guard (AD-14) — the guard arrives with Story 02.
 *
 * Empty in Story 01. Filled by Stories 07, 12, 13.
 */
export const portalRoutes: Routes = [];

export default portalRoutes;
