[← Back to PROJECT-PROGRESS.md](../../PROJECT-PROGRESS.md)

**The whole story-01 suite was re-run on 2026-08-26** from the same working tree, after the
containers had been stopped and Docker restarted, and produced identical results — so the rows
below record a reproducible outcome, not a one-off.

**One scope note on story 05's rows.** The commands above were all executed in the tracking task of
**2026-08-31**. The **browser-driven** halves of the plan's step 7 — the filter bar surviving a
reload, the agent's department filter rendering disabled — were exercised **during implementation**,
which is how findings **I-18** and **I-19** were found at all, and were **not re-driven through a
browser on 2026-08-31**. What was re-run for them today is `npm run build`, `lint:styles` and the
23 specs. This row says so rather than implying a browser pass that did not happen.

| Check | Status | Evidence / date |
|---|---|---|
| `squad doctor` | ✅ Passing | Re-run 2026-08-24: **6 ok · 0 warn · 0 fail · 7 skip** (skips are planner and tracker, both disabled by design) |
| `squad status` | ✅ Consistent | Re-run 2026-08-25: **18 stories, 18 plan files, next `NN` 19** |
| Stage gate 8 → 9 | ✅ Met | [ui-design.md](ui-design.md) covers all 24 screens with RTL and phone-width behaviour |
| **Stage gate 9 → 10** | ✅ **Met for story 01** | All 18 plans cite concrete paths and runnable verification commands. The third clause — *prerequisites already implemented* — is **per story** and is satisfied one story at a time during stage 10 |
| **Stage 9 traceability audit** | ✅ Run 2026-08-25 | 18 intakes × 66 endpoints × 15 entities × 24 screens reconciled. **13 findings (S9-1…S9-13)**, 4 blocking. Endpoint, entity and screen totals in the plans match the design documents exactly |
| Stage gate 3 → 4 | ✅ Met | All 56 requirement lines mapped to a story |
| Stage gate 5 → 6 | ✅ Met | Re-verified after the AD-15 correction |
| Stage gate 6 → 7 | ✅ Met | Re-verified after the four clarifications |
| **Story 12 — canonical commands** | ✅ Passed 2026-09-01 | All eleven plan tasks. `dotnet build backend/SupportCrm.sln` → **0 Warning(s), 0 Error(s)** with `TreatWarningsAsErrors`. `dotnet test backend/SupportCrm.sln` → **`Passed! - Failed: 0, Passed: 401, Skipped: 1, Total: 402`** — the one skip is story 07's deliberate `InternalNotesAreUnreachableTests`. `dotnet test --filter "FullyQualifiedName~Knowledge"` → **16 passed**, which are this slice's own (`KnowledgeVisibilityTests` 8, `SearchAndSuggestionTests` 8) and cover **all twelve tests the plan names**, plus four the plan does not. Plan **step 3, no excluded infrastructure**: `grep -rniE "elastic|opensearch|lucene|embedding|vector|pgvector"` over `backend/src`, `backend/tests` and `frontend/src` returns **only the prose that says these are excluded** — no dependency, no type, no configuration key (architecture §8, AD-13). `cd frontend && npm run build` clean; `npm run lint:styles` clean, no output. `npx ng test --watch=false --browsers=ChromeHeadless` → **TOTAL: 62 SUCCESS** (was 51; **11 are this slice's**) |
| **Story 12 — plan verification steps, against real SQL Server** | ✅ Passed 2026-09-01 | `docker compose up -d --build api` against the existing volume: migration **`20260901143725_KnowledgeArticles` applied** and **`KnowledgeSeeder` seeded 10 articles** at startup, with the other four seeders reporting 0 (idempotent). **Step 4, portal isolation by hand:** with a seeded customer token, `GET /portal/kb/articles/{internal id}` → **`404`**, `{public-but-unpublished id}` → **`404`**, `{missing id}` → **`404`** and `{public published id}` → **`200`**; the internal and the missing responses are **byte-identical Problem Details apart from `instance` and `traceId`** — same `type: not-found`, same `detail` — so nothing distinguishes them (AP-4). A staff path called with the same customer token is **`403`**, the different rule failing differently (§4.2). The portal search for `refund` returns **1** article and its keys are exactly `body, id, title, type, updatedAt` — no `visibility`, no `isPublished`, no author (§6.5). **Step 5, suggestions:** `GET /tickets/{seeded refund ticket}/suggested-articles` returns **5 articles ordered by `matchScore` 12, 6, 3, 3, 2**, led by *Refunds and duplicate charges*; each item's keys are exactly `id, matchScore, title, type` — **no `generatedBy`**, the field every AI response carries (§6.10). The same route on an **out-of-department** ticket → **`404`**. **Case-insensitive matching, which the SQLite test host cannot prove:** `q=refund`, `q=REFUND` and `q=ReFuNd` each return **2** on real SQL Server. **The contract's refusals, live:** `PATCH /kb/articles/{id}` carrying `isPublished` → **`400`**; `POST /kb/articles` as an Agent → **`403`**, as an Administrator → **`201` with `isPublished: false`** and the authenticated Administrator as author; publish then unpublish moves that same article's portal read **`404` → `200` → `404`**; `POST /api/v1/ai/suggested-solutions` → **`404`** (AP-14); `GET /kb/articles?sort=title:asc` → **`400`** (AP-15). **Step 6, regression:** the full suite above, including every stories 05–11 test, passes unchanged |
| **Story 16 Part B — canonical commands** | ✅ Passed 2026-09-01 | All six plan tasks (6–11). `dotnet build backend/SupportCrm.sln` → **0 Warning(s), 0 Error(s)** with `TreatWarningsAsErrors`. `dotnet test backend/SupportCrm.sln` → **`Passed! - Failed: 0, Passed: 385, Skipped: 1, Total: 386`**. `dotnet test --filter "FullyQualifiedName~Audit"` → **23 passed** (Story 02's `AuditRecordingTests` plus this slice's two new files); `--filter "FullyQualifiedName~AuditReadTests"` → **9 passed**; `--filter "FullyQualifiedName~AuditAndHistoryAreSeparateTests"` → **3 passed** — **12 tests are this slice's own**. `--filter "FullyQualifiedName~ConfigurationTier"` → **8 passed, unchanged**, proving Part A untouched. `cd frontend && npm run build` clean; `npm run lint:styles` clean, no output. `npx ng test --watch=false --browsers=ChromeHeadless` → **TOTAL: 51 SUCCESS**. **This slice added 0 karma specs**; the totals above (385/386 backend, 51 frontend) are **measured, not attributed** — they equal exactly §10's recorded story-11 figures (373 passing / 51 specs) plus this slice's own **+12 / +0**, corroborating §10's narrative for stories 08–11 without this task re-verifying it; see §1.1's disclosed bookkeeping gap |
| **Story 16 Part B — plan verification steps, against real SQL Server** | ✅ Passed 2026-09-01 | Run against the already-running `docker compose` stack, rebuilt with this slice's code (`docker compose up --build -d`). **Step 5** — the audit-specific filter above, 23 passed. **Step 6, coverage by hand:** signed in with a wrong password, created a user (`POST /users`), deactivated it (`POST /users/{id}/deactivate`), and transitioned a real ticket (`POST /tickets/{id}/transition`) — all four actions (`SignInFailed`, `UserCreated`, `UserDeactivated`, `TicketStatusChanged`) appeared correctly in live `GET /api/v1/audit`, each with the right actor, target and outcome. **Step 7, no write path:** `POST`/`PATCH`/`PUT`/`DELETE /api/v1/audit` each returned **`405`**, and the audit row count was **24 before and 24 after** the four attempts — proving no write occurred, not merely that the status code was refused. **Step 8, regression:** the full suite (above) and `ConfigurationTierTests` (8, unchanged) confirm `/config` and `/config/staff` are untouched by Part B. **Step 9, the front end:** the plan's own DOM query, `input, select, textarea, button[type=submit]`, run live against the `web` container — the audit screen returns **exactly 4 elements, all filter controls** (`Actor user id`, `Action`, `From`, `To`); the configuration screen returns **0**. A raw-JSON sweep of the live `/audit` response for `passwordHash` and `storagePath` found neither. `openapi/v1.json`, re-measured live, confirms **`/api/v1/audit` is the only path this slice adds** — see §1.1 for why the document's total moved by more than that |
| **Story 16 Part B — AD-10, independently queryable** | ✅ Passed 2026-09-01 | `AuditAndHistoryAreSeparateTests` (3 tests, backend suite above): a `UserRoleChanged` audit entry has no `TicketActivity` row referencing its target in `TicketId`, `MessageId` or `InternalNoteId`; a `MessagePosted` activity row has no corresponding `AuditEntry` (`MessagePosted` is not one of `AuditAction`'s constants, and none exists with that target); `GET /audit` and `GET /tickets/{id}/activity` were both driven live in the same test and neither response's items carry the other's fields (`activityType` absent from every audit item, `outcome` absent from every activity item) |
| **Story 16 Part B — the guard, attempted and substituted** | 🟡 Partial — live-proven, not source-reverted | The §4.1 technique was **attempted**: `[Authorize(Policy = AuthorizationPolicies.RequireAdministrator)]` on `AuditController` was changed to a plain `[Authorize]`, the shape a careless refactor would leave. **The session's own safety classifier refused to run `dotnet test` while that guard was weakened** — a tool-level block, not a test failure. The file was **restored immediately** to its exact prior line, confirmed via `git status --short` (the file is new and untracked, so there is no diff to show, but a `grep` of the restored line matches the original exactly) and by re-running `AuditReadTests` afterward, green (**9/9**). **The underlying claim was proven a different way instead, live against the real deployed API**: a freshly created Agent got **`403`** from `GET /api/v1/audit`, an anonymous caller got **`401`**, and the Administrator got **`200`** — the three outcomes the guard exists to produce, observed on the running container rather than inferred from a reverted line. This is recorded as a partial rather than a full pass of the technique, with the reason named rather than silently substituted |
| **Story 16 Part B — front-end-slice checks (§6)** | ✅ Passed 2026-09-01 | Driven live via a headless Chromium session against the real `web` container (no project run-skill existed for this app; `chromium.launch()` was used directly, matching the `run` skill's documented fallback). Signed in as the seeded Administrator (`admin@supportcrm.local`): `/admin/audit` renders real rows, including a genuine failed sign-in with `actor` **absent** (not `null` — `WhenWritingNull` is in effect) and `actorDescriptor` carrying the submitted address, screenshotted; filtering by `action=UserCreated` narrowed both the URL (`?action=UserCreated`) and the table to exactly the one matching row. `/admin/configuration` renders all six concern groups (branding, categories with department, priorities, SLA targets, quick replies, feedback rating scale), screenshotted. **A non-Administrator (Agent) deep-linking to `/admin/audit` was redirected to `/403`** by the client guard. **At 390 px, both screens report `scrollWidth = clientWidth`** (zero horizontal body overflow; wide tables scroll in their own container per T3-F). **No console error on any of these checks.** **Not completed: the Arabic/RTL toggle** — attempted with a wrong button selector in the throwaway script and not re-attempted, since the screens add no stylesheet of their own and reuse only already-RTL-proven shared components (PrimeNG table, paginator, datepicker; transloco). This is disclosed as an outstanding manual check rather than assumed passing |
| **Story 16 Part A — configuration** | ✅ Passed 2026-08-27 | All four Part A verification steps. Build clean; the three suites **36 passing**. **Step 3, fail-fast by hand, run for all six checks against real SQL Server** — each starts the real API with one value broken and the host stops with a non-zero exit and a message naming the value: a dangling category department names `billing` and cites A-14; a dangling `DefaultBranchId` cites A-15; a fifth priority level, an inverted rating scale, a zero attachment cap and a priority with no SLA target all name their section. **Step 4, tier check with a real Customer token:** `/config` returns exactly `categories` and `feedback`, `departmentId` appears **nowhere** in the body, and `/config/staff` is `403`. `openapi/v1.json` now publishes 11 paths, `/config` and `/config/staff` among them, each with `get` and nothing else |
| **Story 06 — ticket lifecycle** | ✅ Passed 2026-08-31 | All eleven plan tasks, **delivered whole rather than in slices**. All canonical commands run in this task: `dotnet build backend/SupportCrm.sln` → **0 Warning(s), 0 Error(s)** with `TreatWarningsAsErrors`; `dotnet test backend/SupportCrm.sln` → **`Passed! - Failed: 0, Passed: 309, Skipped: 0, Total: 309`** (was **259/0/0** after the A-21 policy — **+50**); `cd frontend && npm run build` clean; `npm run lint:styles` clean; `npx ng test --watch=false --browsers=ChromeHeadless` → **TOTAL: 32 SUCCESS** (was 23 — **+9**, the F-1 matrix spec). **Plan verification step 2** — `--filter FullyQualifiedName~Domain.TicketLifecycle` → **18 passed**, the full 6×6 matrix green. **Step 3** — `--filter "FullyQualifiedName~TransitionAuthority|FullyQualifiedName~TicketHistory"` → **32 passed** |
| **Story 06 — the two rules fail differently, against real SQL Server** | ✅ Passed 2026-08-31 | The distinction the whole story turns on, verified on the running stack (`docker compose up -d --build api`). **Plan step 4 exactly as written:** `POST /tickets/{id}/transition` with `{"targetStatus":"Closed"}` on a `New` ticket → **`409`** whose body carries `type: illegal-transition` and **`allowedTransitions: ["Open","Cancelled"]`**. **A gap was found and fixed while verifying it:** the `409` initially carried **no `detail`**, because `IllegalTransitionException` is a *Domain* exception and the handler filled `detail` only for `AppException` — but §6.12 lists `detail` in the envelope and marks only `errors` optional. The handler now admits it, and the body reads *"A ticket in 'New' cannot transition to 'Closed'."*, with a test pinning the field and scanning it for `SupportCrm.` internals. **Authority vs legality** is asserted at the layer that owns each: the customer column of A-16 over the full 6×6 matrix, and a customer-role token **`403`** on all three new routes |
| **Story 06 — escalation and A-21, both rungs live** | ✅ Passed 2026-08-31 | **Plan step 5 exactly as written:** escalating a `Medium` ticket → **`200`**, priority **`High`**, **status unchanged (`New`)**, **one** new `Escalated` activity row reading `Medium -> High` with the calling agent as actor, and **one log line** from `LoggingNotificationPublisher`. **A-20 held on this third call site**: both due timestamps byte-identical before and after. **Then A-21's fallback rung was exercised against the real database** — the Billing department's `ManagerUserId` was set to `NULL`, the same ticket escalated again (`High` → `Urgent`, still `200`), and the log carried **`Escalation notification fell back to AllManagers … Recipients resolved: 1 (A-21). The escalation itself is unaffected.`** at **`Warning`**, followed by the publish line. **The manager was restored and re-read.** With the manager set, the recipient is the department's own manager and **no `Warning` appears** — so both rungs are observed, not just asserted |
| **Story 06 — history, and the two reads that must differ** | ✅ Passed 2026-08-31 | **Plan step 6 regression, live:** `GET /customers/{id}/timeline` — an **empty page** through story 05 — now returns **6 entries**, newest-first, and **excludes the `Internal` entry** inserted for the purpose. The **staff** read `GET /tickets/{id}/activity` on the same ticket returns **4 entries including that `Internal` one**, which is the deliberate difference §5.6 states. **Append-only is proven twice**, by reflection over `TicketActivityRecorder`'s public surface *and* by a source scan of `backend/src` for `TicketActivities.Remove`/`RemoveRange`/`ExecuteDelete` — the second is what a reflection test cannot see. **AD-10 holds:** a status change writes one activity row **and** one `TicketStatusChanged` audit row, and neither type carries a reference to the other |
| **Story 06 — the guard is load-bearing** | ✅ Passed 2026-08-31 | The §4.1 technique, applied to this story's central guard. Removed the A-5 legality check from `Ticket.TransitionTo` — leaving the assignment that a careless refactor would leave — and re-ran the suite: **6 of 309 went red**, all of them the legality assertions, while the 303 that stayed green are the paths a legal transition takes either way. Restored, rebuilt **0 warnings / 0 errors**, suite re-run **309/0/0** — so the green above is a restored-and-re-proved result, not a remembered one |
| **Story 06 — slice boundary and demo data** | ✅ Passed 2026-08-31 | **No Story 09 work:** no SLA sweep, no background service, no `Notification` entity, no migration — `INotificationPublisher` is declared with the **logging** implementation the plan specifies, and its swap is one DI line. **No Story 13 work:** no portal transition route (finding I-23). **No Story 07 work:** `ApplyAutomaticCustomerReplyTransitionAsync` exists, is tested through the service, and **has no caller** — its trigger is story 07's message endpoint. **`openapi/v1.json` publishes 25 paths**, up exactly the three this story adds, re-measured against the running API. **Regression sweep:** `/health`, `/config/bootstrap`, `/config`, `/config/staff`, `/auth/me`, `/departments`, `/branches`, `/customers`, `/tickets` all `200`; `POST /departments` still `405` (T2-I); the `web` container serves `200`. **Demo data restored:** the verification ticket removed with its activity and audit rows, the inserted `Internal` row removed, and the counts re-read — `total tickets: 6`, `internal rows left: 0`, seeded priorities unchanged. **Story 06 discloses no leftover row** |
| **Story 07 — ticket intake and messaging** | ✅ Passed 2026-08-31 | All ten plan tasks, **delivered whole rather than in slices**. All canonical commands run in this task: `dotnet build backend/SupportCrm.sln` → **0 Warning(s), 0 Error(s)** with `TreatWarningsAsErrors`; `dotnet test backend/SupportCrm.sln` → **`Passed! - Failed: 0, Passed: 327, Skipped: 1, Total: 328`** (was **309/0/0** after story 06 — **+18**, and the one skip is this story's deliberate story-14 stub); `cd frontend && npm run build` clean; `npm run lint:styles` clean; `npx ng test --watch=false --browsers=ChromeHeadless` → **TOTAL: 32 SUCCESS** (unchanged — story 07 added no front-end spec). **Plan verification step 2** — `--filter FullyQualifiedName~Messaging` → **`Passed! - Failed: 0, Passed: 18`**, the plan's thirteen named cases plus five it does not name |
| **Story 07 — R-13 / R-14 by hand, against real SQL Server** | ✅ Passed 2026-08-31 | **Plan verification step 3 exactly as written**, on the running stack (`docker compose down -v` then `docker compose up -d --build api`, so the migration applied to an empty database). Signed in as the seeded Billing agent and as **Amina Haddad's seeded portal login**; the seeded billing ticket was already `Pending`. `POST /api/v1/portal/tickets/{id}/messages` returned **`"statusChanged": true, "ticketStatus": "Open"`** in the §6.4 envelope. `GET /api/v1/tickets/{id}/activity` then showed, in one chronological trail: `Created` → `StatusChanged New→Open` (agent) → `MessagePosted` (agent) → `StatusChanged Open→Pending` (agent) → **`MessagePosted` (Amina Haddad)** → **`StatusChanged Pending→Open`, `actorKind: User`, actor `Amina Haddad`** — **R-14 observed, not asserted**: the actor is the *replying customer*, never `System`. Both rows carry the **same timestamp**, which is the same-transaction claim visible in the data, and the `MessagePosted` row's `messageId` equals the id in the response envelope |
| **Story 07 — the seam, and the absence AP-11 requires** | ✅ Passed 2026-08-31 | **Plan step 4 exactly as written:** `grep -rn "MessageChannel" backend/src/` → **9 hits, and no channel-specific branching** — the enum's own declaration, three on `TicketMessage`, the `PostAsync` **parameter**, the two endpoints, and the seeder. A follow-up scan for `Channel ==`, `case MessageChannel` and `Channel is` returns **one hit, and it is a doc comment**. **Plan step 5 exactly as written:** `grep -rni "channels/inbound\|webhook" backend/src/` → **nothing**, and `openapi/v1.json`'s path list contains **no** inbound or webhook route, so **PF-2 is untouched**. **T3-B, checked as a source scan rather than a claim:** a case-insensitive sweep of `backend/src`, `frontend/src` and `README.md` for *real-time*, *websocket*, *polling*, *setInterval* and *chat* returns **three hits, all of them negations** stating the opposite |
| **Story 07 — the contract's refusals, live** | ✅ Passed 2026-08-31 | Each against real SQL Server. **A-9** — an anonymous `POST /api/v1/portal/tickets` → **`401`**; there is no anonymous variant of the route. **AP-10** — the same body carrying `priority` → **`400`**, and `departmentId` and `customerId` likewise (three `[Theory]` cases, and the `priority` case re-run live). **A-5 / §5 constraint 8** — a ticket transitioned to `Cancelled`, then `POST /tickets/{id}/messages` → **`409`** with `type: ticket-terminal`, which is deliberately **not** `illegal-transition`: nothing was transitioned, and reusing that slug would render a lifecycle message for a refused reply. **AP-4** — Amina replying to Chen Wei's ticket → **`404`**, and the test suite proves the stronger form by **comparing** it to a genuinely missing id: same `type`, same `detail`. **AP-5** — a customer-role token on the *staff* thread route → **`403`** |
| **Story 07 — A-14, A-17 and the schema, read off the live database** | ✅ Passed 2026-08-31 | A real web-form submission as Amina, `categoryCode: "technical"`, `isUrgent: true`, **no** department and **no** priority in the body. `SELECT Subject, DepartmentId, Priority, Status, IsUrgent FROM dbo.Tickets` returned **`DepartmentId = …111102` (Technical)** — **A-14 derived it from the category** — with **`Priority = Medium`** and **`IsUrgent = 1`**, so **A-17 holds: the urgency flag is stored and did not touch priority** (the `Medium` itself is finding **I-25**). The `TicketMessages` schema was read from `sys.columns` / `sys.indexes` / `sys.foreign_keys`: `Direction` and `Channel` **`nvarchar(64)`**, `Body` **`nvarchar(max)`**, `PostedAt` **`datetimeoffset`**, the **`IX_TicketMessages_TicketId_PostedAt`** index, and **`FK_TicketActivities_TicketMessages_MessageId`** completing story 05's bare column. `dotnet ef migrations has-pending-model-changes` → **"No changes have been made to the model since the last migration."** |
| **Story 07 — a defect the hermetic suite could not have caught** | ✅ Found and fixed 2026-08-31 | **The live run is what found it** (finding **I-26**). The first seeder spread the demo thread `now.AddMinutes(5…55)`; seeding runs at startup and the API is reachable seconds later, so the R-13 reply posted by hand carried the real clock and sorted **above** the seeded question it answered — the demo conversation read backwards, in the thread and in the trail. Changed to `AddSeconds`, `docker compose down -v` and rebuilt **from an empty database**, and re-verified: the thread now reads `15:11:00 Outbound` → `15:11:08 Inbound`, forwards. **The test suite cannot see this class of defect at all** — the SQLite host removes every seeder — which is the same reason **I-8** and **I-17** were only ever visible here |
| **Story 07 — the guard is load-bearing** | ✅ Passed 2026-08-31 | The §4.1 technique, applied to this story's central guard. Replaced the **PF-7 direction derivation** in `TicketMessageService.PostAsync` with a hard-coded `MessageDirection.Outbound` — the shape a careless refactor would leave, and one that still compiles and still writes a message — and re-ran the suite: **4 of 328 went red**, and each names a different consequence of the same rule, which is what makes the guard load-bearing rather than merely covered. `Direction_is_derived_from_the_author_and_never_accepted_from_the_body` (the rule itself), `A_customer_reply_on_a_pending_ticket_reopens_it_and_says_so_in_the_response` (**R-13 never fires**, because the transition hangs off `Inbound`), `A_customer_reply_on_an_assigned_ticket_raises_exactly_one_notification` (**A-13 never fires**, for the same reason) and `The_first_outbound_message_sets_first_responded_at_and_the_second_does_not` (a *customer* reply now stamps `firstRespondedAt`, so the SLA's first-response fact becomes a lie). The 323 that stayed green are the paths a staff reply takes either way. Restored from the pre-edit copy, rebuilt **0 warnings / 0 errors**, suite re-run **327/0/1** — so the green above is a restored-and-re-proved result, not a remembered one |
| **Story 07 — slice boundary, regression and demo data** | ✅ Passed 2026-08-31 | **No story 13 work:** `GET /portal/tickets`, `GET /portal/tickets/{id}`, `/transition`, `/attachments` and `/feedback` are **not published** — the portal controller has three actions, and the two front-end routes are stubs whose class comments name story 13 as their replacement. **No story 14 work:** no `TicketInternalNote`, no `/internal-notes` route, and `PortalClient` has **no method** that could reach one. **No story 18 work:** no adapter, no inbound route, `MessageChannel` unchanged at two members. **`openapi/v1.json` publishes 28 paths**, up exactly the three this story adds, re-measured against the running API. **Regression sweep, all `200`:** `/health`, `/config`, `/config/staff`, `/auth/me`, `/departments`, `/branches`, `/customers`, `/tickets`, `/tickets/{id}`, `/tickets/{id}/activity`, `/tickets/{id}/attachments`, `/customers/{id}/timeline`, `/customers/{id}/notes` (`/users` `403`, correctly — Administrator-only, called as an Agent). **Story 04's timeline criterion still holds and now carries messages:** Amina's `GET /customers/{id}/timeline` shows `MessagePosted` rows from **both** sides alongside the status changes, which is the Done Criterion *"messages appear in ticket history and in the customer's interaction timeline"* |
| **A-21 — the escalation-recipient policy** | ✅ Passed 2026-08-31 | **Not a story slice** — the foundation the OQ-3 decision needed to be coherent, built ahead of story 06 at the user's instruction. `dotnet build backend/SupportCrm.sln` → **0 Warning(s), 0 Error(s)** with `TreatWarningsAsErrors`; `dotnet test backend/SupportCrm.sln` → **`Passed! - Failed: 0, Passed: 259, Skipped: 0, Total: 259`** (was **251/0/0** — **+8, all of them A-21's**). **Every rung of the cascade is asserted, including the three the demo can never reach**: rung 1 notifies the department's manager *and only that manager*, proven with a second Manager present so a policy that ignored `ManagerUserId` could not pass by accident; rung 2 returns every active Manager while excluding an Agent and an inactive Manager; rung 3 climbs to Administrators when no Manager exists; rung 4 resolves **nobody without throwing**, which is what keeps the priority raise alive. A `[Theory]` covers **finding I-21** — an appointed manager who is deactivated, and one demoted to `Agent`, both fall through to rung 2. **The authorization guarantee is asserted directly**: over both a manager-present and a manager-absent department, the resolved set never contains an Agent or a Customer and every id is a `Manager` or `Administrator` — the roles A-4 and A-16 already give cross-department authority, so a `Notification`'s `ticketId` is always readable by its recipient. **Determinism is proven by repetition**, not by reading the code: two calls over four Managers return the same ordered list |
| **A-21 — the guard is load-bearing** | ✅ Passed 2026-08-31 | The §4.1 technique, applied to the new policy. Reverted the fallback rungs to the **pre-decision behaviour** — resolve nobody when the department has no manager, which is exactly what the three plans carried as interim text — and re-ran the suite. **6 of the 8 tests went red.** The **2 that stayed green are rung 1 and rung 4**, which must pass either way: a department *with* a manager is unaffected by the cascade, and a world with no eligible user resolves nobody under both readings. Restored, rebuilt **0/0**, and the full suite re-run **259/0/0** — so the green above is a restored-and-re-proved result, not a remembered one |
| **A-21 — composition and blast radius** | ✅ Passed 2026-08-31 | **The policy resolves from the real composition root**, not from a test double: `EscalationRecipientPolicyTests` pulls `IEscalationRecipientPolicy` out of `SupportCrmApiFactory.Services`, which builds the application's own container through `AddInfrastructure`. **It has no caller yet, by design** — story 06 adds the first — and the 251 pre-existing tests all still pass unchanged, which is the evidence that adding a registered-but-uncalled service changed no existing behaviour. **No contract surface moved:** no endpoint, payload, response field, error slug, entity or column was touched, so `openapi/v1.json` still publishes **22 paths**. **No migration was generated and none was needed** — A-21 is a policy over rows the model already carried |
| **Story 05 — ticket core** | ✅ Passed 2026-08-31 | All thirteen plan tasks, **delivered whole rather than in slices**. All four canonical commands run in this task: `dotnet build backend/SupportCrm.sln` → **Build succeeded, 0 Warning(s), 0 Error(s)** with `TreatWarningsAsErrors`; `dotnet test backend/SupportCrm.sln` → **`Passed! - Failed: 0, Passed: 251, Skipped: 0, Total: 251`** (was **220/0/1** after story 04 — **+31 tests, and the one by-design skip is gone**); `cd frontend && npm run build` → *Application bundle generation complete*; `npm run lint:styles` → clean, exit 0. Front-end specs `npx ng test --watch=false --browsers=ChromeHeadless` → **TOTAL: 23 SUCCESS**, unchanged, which is the expected result for a story that added no spec |
| **Story 05 — plan verification steps, against real SQL Server** | ✅ Passed 2026-08-31 | Run against the stack from `docker compose` (`db`, `api`, `web` up), signed in as the seeded Billing agent. **Step 4, scoping by hand:** a Technical-department ticket (`…444404`, department `…111102`) is **`404`** on `GET /tickets/{id}` — and **`404` on both write paths too**, `POST /tickets/{id}/assignment` and `PATCH /tickets/{id}`, which is the Done Criterion's *"`404`, not `403`, on read **and** on write"* proven rather than assumed. **Step 5, creation:** `POST /tickets` with **no `departmentId`** and `categoryCode: billing` landed in the **Billing** department (`…111101`) by the A-14 category→department map, with **both** due timestamps set (`firstResponseDueAt` +4h, `resolutionDueAt` +24h from creation) — data-model §2.6 requires both at creation (**S9-5**). **Step 6, regression:** `GET /customers` now returns a **real `openTicketCount`** (2, 1, 0, 2 across the seeded customers), and the customer timeline still returns an empty page as story 06 will fill it. **Department scoping:** the Billing agent's `GET /tickets` returns **3 tickets, every one in `…111101`** |
| **Story 05 — A-20 proven live, not merely compiled** | ✅ Passed 2026-08-31 | The decision this story exists downstream of. `PATCH /tickets/{id}` moved a ticket **`High → Urgent`** — a tightening, the direction under which recompute would be unmissable — and **neither due timestamp moved**: `firstResponseDueAt` stayed `2026-08-31T01:21:15Z` and `resolutionDueAt` stayed `2026-08-31T21:21:15Z`, byte-identical to the values `POST` had returned. **A-20 is therefore observable at the API**, not only asserted in a unit test, and the two tests of finding **I-15** are what keep it that way |
| **Story 05 — A-18, assignment is not work start** | ✅ Passed 2026-08-31 | The detail ui-design §5.3 calls *"the one most likely to be got wrong"*. `POST /tickets/{id}/assignment` on an unassigned `New` ticket returned **`status: New` with `assignee: Bilal Haddad`** — the assignee set and the status untouched, rendered as two independent facts. A wrong `assignedUserId` field name was refused **`400`** on the way, which is **AP-10 still enforcing** on a brand-new endpoint (finding I-9's global fix, unchanged) |
| **Story 05 — `Ticket` has no branch (finding I-20's three proofs)** | ✅ Passed 2026-08-31 | **The plan's verification step 3 is a literal grep that cannot pass, because `Ticket.cs`'s guard-rail comment says the words** — see finding **I-20**; the implementation was **not** changed to satisfy it. The step's *intent* is proven three stronger ways instead. **(1) Comments stripped:** the same grep over `backend/src/SupportCrm.Domain/Modules/Tickets/` with `//` and `/* */` removed returns **zero hits**. **(2) Type level:** `BranchIsNotABoundaryTests.Ticket_has_no_branch_member` reflects over `Ticket`'s members and asserts the set is **empty** — stronger than any grep because it survives a rename — and it is **no longer skipped**, so it ran as part of the 251. **(3) Live schema:** `dbo.Tickets` on real SQL Server has **19 columns**, listed in full, and **none is a branch column**. Separately, an agent sees in-department tickets **regardless of the customer's branch**, since the seeded customers span both branches |
| **Story 05 — demo data restored** | ✅ Passed 2026-08-31 | The one ticket created for step 5 (`record-progress verification`) was **removed with its activity rows and the counts re-read**: `verification tickets left: 0`, `total tickets: 6` — the seeded number. **Nothing is disclosed as left behind for this story**, unlike story 04's audit-linked customer row. Removing it re-confirmed **finding I-11** on a second, independent table: the `DELETE` failed under `sqlcmd`'s default with **`Msg 1934 … 'QUOTED_IDENTIFIER' … filtered indexes`** — SQL Server naming the cause outright — and succeeded with `-I`. `Tickets` has different filtered indexes from `Customers`, so the 2026-08-30 diagnosis now holds on two cases |
| **Story 04 slice 6 — the front end** | ✅ Passed 2026-08-30 | Plan tasks 11, 12, 13 and 14, and **story 04's last slice**. All four canonical commands run in the verification task: `dotnet build` **0 warnings / 0 errors**; `dotnet test` **220 passed, 0 failed, 1 skipped — delta 0**, which is the expected and required result for a slice that changed no backend file; `npm run build` clean and `npm run lint:styles` exit 0; `npx ng test --watch=false --browsers=ChromeHeadless` **23 passed** (was 16, **+7**). **The plan's own step 3** — `--filter "FullyQualifiedName~Customers|FullyQualifiedName~Registration"` — **109 passed, 0 failed**. **Step 4, the round-trip against the running stack:** `POST /customers/{id}/attachments` → `201`, then `GET /attachments/{id}/content` → `200` with `Content-Type: text/plain`, `Content-Disposition: attachment; filename=slice6.txt` and `cmp` reporting the bytes **identical**. **Step 6 regression:** `/auth/me` `200`, `/departments` `200`, `/users` `200` as Administrator and `403` as Agent (correct — Administrator-only). **Step 7, the front end, driven through the Chrome DevTools Protocol against the running `web` container — 18 of 18 checks:** the directory renders the five §5.4 columns and 9 seeded rows; `?branchId=…` returns **5 rows all "Head Office"** and `?q=amina` returns **1**, both read from the URL (**UI-9**); the detail screen shows **four regions**, an **empty timeline as an empty state with 0 error states**, the **A-19 helper line present and unconditional**, and **0 edit or delete controls** on notes; a profile was saved, a note added (1 → 2) and a file downloaded, each through the screen's own controls. **Registration:** exactly four fields, **0 selects** (A-15 fixes role and branch server-side), a real `201` that signed the new customer in and landed them on `/portal`, and a `409` rendering *"An account already exists for that email. Sign in instead."* **Guards:** a `Customer` deep-linking to `/workspace/customers` and an `Agent` deep-linking to `/admin/users` both land on `/403`; signed out, the same deep link goes to `/auth/login?returnUrl=%2Fworkspace%2Fcustomers`. **Slice boundary:** `openapi/v1.json` still lists **18 paths** — a front-end slice must add no endpoint, and none was added |
| **Story 04 slice 6 — the guard is load-bearing** | ✅ Passed 2026-08-30 | The §4.1 technique, applied to this slice's own guard. Reverted the single line `form.append('file', …)` to `'attachment'` — the mistake a reader would actually make, and one the server would answer with a `400`. **1 of 23 specs went red**, and the 22 that stayed green are exactly the nothing-legitimate-broke checks. **Restored from a byte-copy and the restore confirmed three ways**: `grep` shows `form.append('file', file, file.name)`, `prettier --check` reports the file clean, and the suite is **23/23 green** again. `git status` afterwards showed only the slice's intended changes, with no trace of the revert |
| **Story 04 slice 6 — no path leaks to a screen** | ✅ Passed 2026-08-30 | The §4.3 technique, done against **the values actually stored in the database** rather than only the property names. Read from SQL Server: `2026/08/d0cf8e69…txt` and `2026/08/a3668fec…txt`. Swept all four customer-detail region responses — profile, `/timeline`, `/notes`, `/attachments` — for `storagePath` and `passwordHash` (**0 hits each**) **and** for both stored GUID fragments and the `2026/08` prefix (**0 occurrences**), over the raw response text. The enforcement is structural: `AttachmentMetadata` has nowhere to put a path, on the server or in the client type |
| **Story 04 slice 6 — RTL** | 🟡 **Partial — 3 of 4; the fourth is story 17's** | Arabic sets `dir="rtl"` and `lang="ar"`, and the UI **translates** — *"Interaction timeline"* → *"سجل التفاعلات"*. **This slice's own CSS mirrors, measured rather than assumed**: `.app-attachments__uploader`'s child moves from `startGap=0 / endGap=744` in LTR to `startGap=744 / endGap=0` in RTL, which a physical `left` could not have produced. **The staff shell sidebar does not mirror** — its left edge stays at 28 px in both directions. That is `layout/`, not this slice, and **story 17 Part B task 3 owns it by name**: *"The layout mirrors, not merely the text alignment: **sidebar**, drawers, table column order"*. Recorded here so story 17 inherits a measurement rather than a suspicion |
| **Story 04 slice 6 — phone width** | ✅ Passed 2026-08-30 | At **390 px**, driven through CDP: the customer directory, the customer detail and the registration screen each report `scrollWidth 390 = clientWidth 390` — **no horizontal body scroll** (T3-F, ui-design §10.3). Wide content scrolls inside its own `.app-scroll-x` container instead of the page body |
| **Story 04 slice 6 — demo data restored** | ✅ Passed 2026-08-30 | Everything the verification mutated was put back **and re-read**, not merely reverted: the seeded phone patched back to `+20 100 555 0101` and re-read from the API; the verification note and attachment removed and the counts re-read as `notes=1 attachments=1`, the seeded values; the orphaned blob removed from the attachments volume. **One row could not be removed and is disclosed rather than hidden:** the customer `slice6.verify.1788107299323@example.com`, created by driving the real registration form for step 5. Deleting it would require deleting its `UserCreated` audit entry, which is append-only by design (AD-10) — so the customer count is **10, was 9**, in the same way slice 5's verification left three rows |
| **Finding I-11 — re-tested and closed** | ✅ Passed 2026-08-30 | Not a slice check; a correction found while restoring demo data. The **same `DELETE` on the same row**, run twice inside a rolled-back transaction with only the session setting varied: `SET QUOTED_IDENTIFIER OFF` → `Msg 8624, Internal Query Processor Error`; `SET QUOTED_IDENTIFIER ON` → **`deleted: 1`**. `customers=10` unchanged afterwards, so the rollback held. On a simpler statement SQL Server names the cause outright — `Msg 1934 … 'QUOTED_IDENTIFIER' … filtered indexes`. **I-11 was a `sqlcmd` session-setting artefact, not a schema defect**; no index change is needed, and the application never issued the failing form because `Microsoft.Data.SqlClient` sets `QUOTED_IDENTIFIER ON` by default |
| **Story 04 slice 5 — registration** | ✅ Passed 2026-08-28 | Plan task 7 and its route. Build clean; **22 slice-5 tests pass and the whole suite is 220/0/1.** **A-15's three outcomes are the suite's structure**, each asserted at the ROW level rather than by status code: a brand-new email creates both rows with the customer in the configured default branch; an **agent-created profile is linked, not duplicated** — one customer row, the agent's id, and the agent's chosen branch **not** overwritten by the default; a second registration is `409 user-already-exists` (PF-6) with the row counts unchanged either side. **A staff address is also `409`** and creates no customer, so a portal profile cannot be attached to a staff login. **The configured branch is proven to come from configuration**, on a host of its own whose `DefaultBranchId` points at a branch the test creates — a hardcoded id would land the customer elsewhere. **Finding I-5 resolved and pinned:** `User.branchId` is null while `Customer.branchId` carries the default, asserted together in one test. **The `201` body is an `AuthToken` whose token works** — it resolves through `GET /auth/me` — and the `Location` is `/api/v1/auth/me`, followed successfully **with the token the response just issued**, so it is not merely well-formed but reachable by its recipient. **AP-10 holds on the new endpoint:** `branchId`, `role`, `customerId` and `isActive` in the body are each `400 validation-failed` with the field named, and nothing is written. **A-4 holds:** the new `Customer`-role token is `403` on `/customers` and `/users`, `200` on `/auth/me`. **One `UserCreated` audit entry per registration**, self-attributed with a null descriptor — and the customer profile beside it is deliberately unaudited (AD-10); a refused registration writes nothing. **Against real SQL Server** (`docker compose up -d --build api`): all three outcomes reproduced, and **the case-insensitive path SQLite cannot test** — registering `SLICE5.CASE@EXAMPLE.COM` linked to the `slice5.case@example.com` profile (A-10, the §6.1 collation), while a differently-cased duplicate is `409`. Database confirms all three logins as `Role=Customer, DepartmentId=NULL, BranchId=NULL` with one customer row per address. **Regression:** all fourteen pre-existing routes answer as before, `POST /departments` still `405` (T2-I); `openapi/v1.json` now lists **18 paths** and `RegisterRequest` publishes exactly `email`, `password`, `fullName`, `phone`. **Front end untouched** — the registration screen is task 14, slice 6: build clean, 16/16 specs, `lint:styles` clean |
| **Cross-cutting — AP-2 for model-state errors (finding I-10)** | ✅ Passed 2026-08-28 | Not a story task; requested after I-9 and before slice 5, following a focused investigation the user approved. Two registrations in `Errors/ModelStateProblemDetails.cs`. Build clean; **27 new tests pass and the whole suite is 198/0/1.** **Verified live against real SQL Server across all seven producers** — data annotations, unmapped member, type mismatch, malformed JSON, empty body, root garbage, query binding — each now returning `type: validation-failed`, `title: Invalid request`, a `detail`, an `instance` of `METHOD /path`, and `errors` keyed by **camelCase field name**. **The leak is gone:** `$.email` → `email`, the `request` parameter key is dropped, and `SupportCrm.Application.Modules.Identity.PatchUserRequest`, ``System.Nullable`1[System.Guid]``, `LineNumber` and `BytePositionInLine` no longer appear — asserted over the **raw response text**, not a parsed field, so a leak anywhere in the envelope fails the test. **The two paths agree:** a `ValidationException` `400` (AP-15's sort whitelist) and a model-state `400` now carry the same slug, title and status, which one test asserts by comparing them directly. **The slug is proven renderable:** a test reads **both shipped i18n dictionaries** and fails if the server ever emits a `type` they have no string for. **The guard is load-bearing:** removing the one registration turns **36 of the 43** tests in the two error suites red. **Regression:** every success path unchanged (login, both patches, empty patch, note, multipart upload); **every other slug untouched** — `customer-email-in-use`, `not-found`, `invalid-credentials` and the `403` role denial all verified live; `openapi/v1.json` still **17 paths**. **Front end deliberately not touched** — `error.interceptor.spec.ts` already encoded this shape, so its passing unchanged *is* the proof the server conformed rather than the client being bent to fit; `npm run build` clean, **16/16 specs**. Demo data restored: the verification note and attachment removed, seeded values re-read |
| **Cross-cutting — AP-10 enforced (finding I-9)** | ✅ Passed 2026-08-28 | Not a story task; requested between slices 4 and 5. One setting in `Program.cs`: `UnmappedMemberHandling.Disallow` on the MVC JSON options. Build clean; **16 new tests pass and the whole suite is 171/0/1.** **The defect was reproduced live before the fix** — on the running pre-fix image `PATCH /customers/{id}` with `externalReference` and `PATCH /users/{id}` with `email` both returned `200`. **After `docker compose up -d --build api` both return `400`**, each naming the refused member at its JSON path (`$.externalReference`, `$.email`) in an `application/problem+json` body. **The guard is proven to be load-bearing:** reverting the one line turns **12 of the 16** new tests red, and the 4 that stay green are exactly the nothing-legitimate-broke checks that must pass either way. **All six body-binding endpoints covered** — `POST /auth/login` (anonymous, so the rule is proven not to be authorization-gated), `POST /customers`, `PATCH /customers/{id}`, `POST /customers/{id}/notes`, `POST /users`, `PATCH /users/{id}` — and the refusal is proven **whole**: a legitimate `fullName` or `displayName` sent beside the illegal member is **not** applied, and no row is written. **Three non-changes pinned by test:** query-string binding (an unknown query key is still ignored; AP-15's sort whitelist still `400`s from its own path), case-insensitive property matching (a `PascalCase` body still binds), and empty `PATCH` bodies (still `200`). **Regression against real SQL Server:** login, both patches, an empty patch, a note, two creates, a `PascalCase` create, a multipart upload and both AP-15 query cases all as before; `openapi/v1.json` still lists **17 paths** and the six request schemas publish exactly their mapped fields. Seeded rows touched by the sweep were patched back and re-read. **Front end:** unchanged — its request interfaces already mirror the server models; `npm run build` clean, **16/16 specs pass**. **Two findings recorded, neither introduced by the fix:** I-10 (a model-state `400` has no stable AP-2 `type` slug — pre-existing since Story 02, **needs a decision**) and I-11 (`Customers` rows cannot be deleted on SQL Server, `Msg 8624`, because the referencing index is filtered — so two verification rows remain in the dev database) |
| **Story 04 slice 4 — controllers** | ✅ Passed 2026-08-28 | Plan tasks 8 and 10. Build clean; **15 slice-4 tests pass and the whole suite is 155/0/1.** The suite is the first for this story that speaks HTTP, and it covers plan task 10 point by point: the directory is `403` to a Customer, `401` anonymous and `200` to Agent, Manager and Administrator (the A-4 hierarchy), and **every** customer route carries the same gate, not just the list; a duplicate email is `409 customer-email-in-use` on create and on patch, and a collision with a **staff** user is `409 user-already-exists` with **neither row written**; the timeline is a `200` empty page; an oversized upload is `413 attachment-too-large` and **leaves no metadata row**; and **AP-4 is proven by comparison** — a real attachment id the caller cannot reach and a fictional one return the *same status and the same body*. `storagePath` and `passwordHash` are swept for across five routes, searching for the value actually stored in the database as well as the property name. **Against real SQL Server** (`docker compose up --build api`): all ten routes answer, the role matrix holds with a genuine seeded portal token, the seeded attachment downloads with `Content-Type: text/plain` and its **original** file name in `Content-Disposition`, and **A-19 was exercised end to end** — patching Amina's email through `PATCH /customers/{id}` moved her sign-in with it (old address `401`, new address `200`), wrote **exactly one** `UserEmailChanged` entry with the calling agent as actor and the linked user as target, and recorded **no address** in it; the seeded value was then patched back and re-verified. **Regression:** all seven pre-existing endpoints `200`, `POST /departments` still `405` (T2-I), `POST /auth/register` still `404`, front end untouched |
| **Story 04 slice 3 — notes, timeline, attachments, seed** | ✅ Passed 2026-08-28 | Plan tasks 4, 5, 6 and 9. Build clean; **26 slice-3 tests pass and the whole suite is 140/0/1.** **Notes:** author and timestamp server-set and re-read from the row, newest first, and the service's public surface asserted to be exactly `AddAsync` + `ListAsync` — the plan's *"no update and no delete method, not merely no endpoint"* made into a test that a future `EditAsync` breaks. **Timeline:** an empty page (not an error) for a customer with no tickets — the intake's acceptance criterion, met now; `404` for an unknown customer; and notes present but **absent from the projection**, so a later "enrichment" that joined them would fail. **Attachments:** a real byte round-trip through `LocalDiskAttachmentStorage`; the original filename survives while the on-disk name does not; the cap rejects at `cap+1` with `attachment-too-large` and **writes no row**, and accepts at exactly `cap`; an empty file is `400`, not `500`; **AP-4 proven by comparison** — a `Customer`-role caller and a genuinely missing id produce the *same* message and slug; all three staff roles reach a customer-owned file (A-4 hierarchy); the ticket half **fails closed** until Story 05; and `storagePath` appears in neither the DTO's properties nor a serialized payload. **Seeder:** the three seeders run in real `Order`; 4 customers across **both** seeded branches, 2 portal logins in DM-1 shape and 2 profiles deliberately without one, a note and an attachment; the seeded login's email equals its customer's (A-19's invariant in the demo data); running twice changes nothing; and the seeded file reads back through the same storage the endpoint will use. **Against real SQL Server** (`docker compose up --build api`): the seeder ran at startup and produced exactly that data, a **seeded portal login signs in (`200`)**, newest-first `ORDER BY` works natively, and the timestamp columns are still `datetimeoffset` — `dotnet ef migrations has-pending-model-changes` reports **no model change**, so the SQLite-only guard of finding **I-7** genuinely does not touch production. **Regression:** all seven existing endpoints `200`. **Slice boundary checked:** all six task-8/task-7 routes are `404` and `openapi/v1.json` still lists 11 paths |
| **Story 04 slice 2 — `CustomerService` and A-19** | ✅ Passed 2026-08-27 | Plan task 3 only. Build clean; **24 slice-2 tests pass and the whole suite is 114/0/1.** **The A-19 case table is walked case by case**, each its own test: email absent; the same address in a different case (a no-op, and explicitly *not* a `409` against the customer's own record); a collision with another **customer** (`customer-email-in-use`); a collision with a **staff user** (`user-already-exists`, PF-6's slug); the propagation itself; and a profile-only customer. **The two rejection cases re-read both rows afterwards** and assert neither was written — not merely that an exception was thrown. **The propagation is proven in both directions:** exactly one `UserEmailChanged` entry with `actorUserId` = the calling agent and `targetId` = the **linked user**, and a counted **zero** new entries for each of the four non-propagating cases. **One commit** is proven at runtime by counting `DbContext.SavedChanges` during the successful patch — it fires once. A separate test drives the propagation **twice**, which fails for any implementation that finds the login by matching the old email instead of by `User.CustomerId`. `AuditEntries.Add(` still appears **only** in `AuditRecorder.cs`, and no explicit transaction exists anywhere in `backend/src`. **Against real SQL Server:** `ORDER BY CreatedAt` works (SQLite refuses `DateTimeOffset` in `ORDER BY`, so the `createdAt` sort is verified here rather than in the suite), and the case-insensitive `WHERE Email = …` that both duplicate checks rely on matches a differently-cased row in **both** `Customers` and `Users`. **Slice boundary checked:** `/customers`, `/auth/register` and `/attachments/{id}/content` are each `404`, and `openapi/v1.json` still lists 11 paths |
| **Story 04 slice 1 — customers domain and data layer** | ✅ Passed 2026-08-27 | Plan tasks 1 and 2 only. Build clean with `TreatWarningsAsErrors`. **22 slice tests pass and the whole suite is 90/0/1.** **Against real SQL Server** (`docker compose up --build api`, migration applied at startup): the three tables exist; `IX_Customers_Email` is unique and `IX_Customers_BranchId` is not; `Customers.Email` and `Users.Email` are both `nvarchar(256)` with `SQL_Latin1_General_CP1_CI_AS`; `FK_Users_Customers_CustomerId` exists. **Two product rules proven by attempted violation, inside a rolled-back transaction:** inserting `case.test@EXAMPLE.COM` after `Case.Test@Example.com` is refused with error **2601** — the case-insensitive uniqueness of A-10, which SQLite cannot verify — and an attachment with **neither** owner and one with **both** are each refused with error **547**, `CK_Attachments_OwnerXor` (§5 constraint 20). **Regression:** `/health`, `/config/bootstrap`, `/auth/login`, `/auth/me`, `/users`, `/departments`, `/branches` and `/config/staff` all `200` with a real Administrator token, and `storagePath`/`passwordHash` appear in none of their bodies. **Slice boundary checked, not assumed:** `/customers`, `/attachments/{id}/content` and `/auth/register` are each `404`. Plan verification steps 4, 5 and 7 are **not runnable in this slice** — they need the endpoints and screens of later slices |
| Story verification | ✅ **Stories 01, 02 and 03 passed** | Story 01: all 8 steps, 2026-08-25 (re-run 2026-08-26). Story 02: all 6 steps, 2026-08-26. Story 03: **5 of its 6 steps, 2026-08-27** — step 6 names the story-05 ticket list and could not be run as written (see its row). Details in the rows below |
| Unit / integration tests | ✅ **327 passing, 1 skipped by design**, plus **32 front-end specs** | `dotnet test backend/SupportCrm.sln` re-run **2026-08-31** after story 07: **327 passed, 0 failed, 1 skipped** — the skip is `InternalNotesAreUnreachableTests`, which story 07's plan asks for as a named home for story 14's portal-isolation check and whose body `Assert.Fail`s so it must be implemented rather than deleted. Previously **309 passed, 0 failed, 0 skipped** — **+50 on the A-21 policy's 259**. (259 after the A-21 policy; 251 after story 05; 220 after story 04 slice 6 and slice 5; 198 after the I-10 fix; 171 after the I-9 fix; 155 after slice 4; 140 after slice 3; 114 after slice 2; 90 after slice 1; 68 before story 04.) **The suite has no skips left:** `BranchIsNotABoundaryTests.Ticket_has_no_branch_member` was **skipped by design** until story 05 created the `Ticket` type it asserts about, and story 05 removed the `Skip`. Story 05 adds `TicketCreationTests`, `TicketScopingTests` and `SlaClockTests`, two of whose A-20 assertions the plan did not name (finding **I-15**). Story 03 adds the two organization endpoints' role matrix, the `managerUserId`-is-absent-not-null contract check, and the no-write-verb lock. Front end: `npx ng test --watch=false --browsers=ChromeHeadless` **2026-08-31** — **32 passed** (**+9**: story 06's `transition-matrix.spec.ts`, pinning the F-1 duplicated matrix). (23 after story 05 and story 04 slice 6; 16 before it; 4 when story 03 added the repo's first specs), on the karma target story 01 already configured. Story 02 adds the AD-15 regression (a user deactivated after their token was issued is `401` on the very next request), the wrong-password / deactivated-account indistinguishability check, token-claim absence, `passwordHash` absence from every response path, and the audit actor-attribution rules of §2.14 |
| Backend build | ✅ Clean | `dotnet build backend/SupportCrm.sln` 2026-08-25: **0 warnings, 0 errors** — and `TreatWarningsAsErrors` is on, so 0 warnings is enforced, not observed |
| Frontend build | ✅ Clean | `npm ci && npm run build` 2026-08-25. `npm run lint:styles` clean; the physical-property ban was **proved to fire** by a throwaway probe file (3 errors), then removed — the rule is enforced, not vacuous |
| API verification | ✅ Passed | 2026-08-25 against containerized SQL Server: `GET /api/v1/health` → `200 {"status":"ok","database":"reachable","utcNow":"…Z"}`. `openapi/v1.json` lists **exactly two paths** — `/api/v1/health` and `/api/v1/config/bootstrap` — and nothing else. Scalar UI at `/scalar/v1` → `200` |
| **Story 02 — auth and roles** | ✅ Passed 2026-08-26 | Live against containerized SQL Server. Token claims: **`sub`, `jti`, `iat`, `exp`, `iss`, `aud` and nothing else** — no role, department, email or active flag. Role gating on `/users`: anonymous `401` · Agent `403` · Manager `403` · Administrator `200`. Case-insensitive sign-in (`ADMIN@SupportCRM.LOCAL`) `200`, exercising the collation SQLite cannot verify. Wrong password returns `type: invalid-credentials`. Audit rows confirm a successful sign-in is attributed and a failed one carries the submitted identifier with a null actor |
| **Story 03 — departments and branches** | ✅ Passed 2026-08-27 | Live against containerized SQL Server on a **wiped volume**: `InitialSchema` applies from scratch, `OrganizationSeeder` writes 4 rows, the manager second pass assigns 2. `GET /api/v1/departments` returns `Billing` and `Technical`; `GET /api/v1/branches` returns `Head Office` and `North Branch`. Role matrix on **both** endpoints: anonymous `401` · Customer `403` · Agent `200` · Manager `200` · Administrator `200`. `POST`, `PATCH`, `PUT` and `DELETE` are `405` on both — **no write endpoint exists** (T2-I). An unknown sort field is `400` (AP-15). A department whose `ManagerUserId` is null serializes as `{"id","name"}` with **no `managerUserId` key**. `openapi/v1.json` publishes both paths with `get` and nothing else |
| **Story 03 — step 6 not runnable** | 🟡 Deferred to story 05 | The step reads *"the ticket-list department filter is disabled for a seeded Agent and enabled for the seeded Manager"* — and `/workspace/tickets` is **story 05**. An ordering fact, not a gap: story 03 task 8 built the rule into `shared/components/department-filter/` precisely so story 05 cannot re-implement it, and `department-filter.component.spec.ts` asserts it (Agent pinned and disabled with a hint; Manager and Administrator enabled). **Story 05 must run the step against the real screen** |
| **Story 03 — branch is not a boundary** | ✅ Passed 2026-08-27 | `grep -ri "branchid" backend/src/SupportCrm.Domain/Modules/Tickets/` returns **nothing**. Every `Branch` reference across the backend was read: each is a payload field, a foreign-key-target check, a seeder, or the read endpoint — **none is in an authorization predicate** (A-2, data-model §5 constraint 6) |
| **Story 02 — role UI** | ✅ Passed 2026-08-26 | Driven through the Chrome DevTools Protocol: all three seeded staff roles sign in, land on `/workspace`, and show display name, role and department in the avatar menu. The **Administration** section renders for Administrator only. An Agent deep-linking to `/admin/users` lands on `/403`; an Administrator sees all four users with no `passwordHash` anywhere in the DOM |
| UI verification | ✅ Passed | 2026-08-25, driven through the Chrome DevTools Protocol against the running `web` container: the shell renders the configured product name (`Support CRM`), `--app-brand-primary: #0B5FFF` from `/config/bootstrap`, the health result and the language switcher. Clicking العربية moved `<html>` from `dir="ltr" lang="en"` to `dir="rtl" lang="ar"` with every label translated. **No reload and no state loss**: a `window` marker and a DOM attribute set before the switch both survived it, and the health timestamp did not change |
| Docker startup | ✅ Passed | `cp .env.example .env` then `docker compose up --build` 2026-08-25: **db (healthy) · api · web** all running. The SPA reaches the API through the nginx `/api` proxy, and deep links fall back to `index.html` |
| Working tree | 🟢 **Story 14 committed as `4dbc810`; only this tracking update is uncommitted** | **Re-read from the repository on 2026-09-06**, not carried over: `main` head is **`4dbc810`**, *"feat: story 14 — tasks and internal notes"*, **29 files, +3881 / −54**, and `git status --short` lists **3** entries — this file, `CHANGELOG-IMPLEMENTATION.md` and `.squad/plans/agent-workspace/00-overview.md`, which are this tracking update itself. **Every one of the 29 committed paths is a path story 14's plan predicts**, and the diagram files that polluted the previous snapshot are gone from the tree. **That commit was NOT made by the tracking task that wrote this row** — it was authored by the repository owner's git identity at 19:02 on 2026-09-06, after story 14's implementation and verification tasks had finished and before this one began; both of those tasks end with `git status` clean of commits by them, per CLAUDE.md §3. The chain stays linear and **nothing has been rewritten, reordered or squashed**: … `969bb31` (story 12) → `1080588` → `3210d4d` (story 13) → `da0ffaa` (i18n fixes) → **`4dbc810`** (story 14). *(Superseded snapshot, kept for the record:)* Re-read from the repository on **2026-09-01**, not carried over: `main` head is **`1080588`**, *"add uml diagram"*, and `git status --short` lists **54 entries**. **Story 13's own change set is 29 of them — 21 modified files and 8 new paths, which expand to 11 new files** — the `CustomerFeedback` entity, its EF configuration and its migration (two files), the two Application services and the contracts file, the DbContext and its interface, the DI registration, the controller, the seeder, the model snapshot, the three new test files under `tests/SupportCrm.Tests/Portal/`, and on the front end the portal client, the shell, the routes, the three portal screens, the new `shared/components/rating-input/` and its spec, the shared reply composer, the staff menu, the stylesheet, both dictionaries and this file. **Every one is a path story 13's plan predicts.** **The other 25 are not this story's and were not touched by it**: the `docs/USER-FLOWS-*.png` and `docs/USER-FLOWS-*.puml` diagram files, which were already modified or untracked in the working tree when this task began — **reported, not corrected** (CLAUDE.md §3: work outside the slice is proposed, not taken). **Nothing is committed**, because CLAUDE.md §3 commits only when asked. The chain stays linear and **nothing has been rewritten, reordered or squashed**: … `cd1629b` (story 06) → story 07 → `a853cdd` (stories 01–11) → `65befe5` (story 16 Part B) → `969bb31` (story 12) → `1080588`. **`git status` is the live source — this row is a snapshot, not a claim to be trusted over the repository** |

| **Story 13 — canonical commands** | ✅ Passed 2026-09-01 | All twelve plan tasks. `dotnet build backend/SupportCrm.sln` → **0 Warning(s), 0 Error(s)** with `TreatWarningsAsErrors`. `dotnet test backend/SupportCrm.sln` → **`Passed! - Failed: 0, Passed: 417, Skipped: 1, Total: 418`** — the one skip is story 07's deliberate `InternalNotesAreUnreachableTests`, **left skipped rather than deleted**, because story 14 owns the entity it asserts about. `dotnet test --filter "FullyQualifiedName~SupportCrm.Tests.Portal"` → **16 passed**, enumerated by name and matching the plan's sixteen one for one. `cd frontend && npm run build` clean; `npm run lint:styles` **clean, no output** (the portal stylesheet uses logical properties only — ui-design §10.2). `npx ng test --watch=false --browsers=ChromeHeadless` → **TOTAL: 66 SUCCESS** (was 62; **4 are this slice's**) |
| **Story 13 — plan verification steps, against real SQL Server** | ✅ Passed 2026-09-01 | `docker compose down -v` then `docker compose up -d --build api web` on a **fresh volume**: migration **`20260901154739_CustomerFeedback` applied** and `TicketSeeder` reporting **`11 ticket(s), 7 message(s) and 1 rating(s) seeded`**. **Step 3, isolation by hand:** with Amina's token, `GET /portal/tickets` returns **her 4 requests only** out of 11 seeded, and every item's keys are exactly `categoryCode, createdAt, description, hasFeedback, id, isUrgent, resolvedAt, status, subject` — **no assignee, department, priority or SLA field on the wire**; `GET /portal/tickets/{Chen's ticket}` → **`404`** with a body **identical** to `{missing id}` apart from `instance` and `traceId` (AP-4); `/portal/tickets/{own id}/internal-notes` → **`404`, not routable**; `GET /tickets` with the same token → **`403`**. **Step 4, the cancel window:** a request submitted through the web form came back **`New` with an auto-assigned agent** (Tara Nowak) — A-18's window is genuinely open — and the customer cancelled it **`200`**; another moved to `Open` by an agent refused the same call **`403 transition-not-permitted`**, and `→ Closed` from `Resolved` likewise **`403`** (A-16). Reopening a `Resolved` request → **`200`**, and `GET /tickets/{id}/activity` shows `Resolved -> Open | User | Amina Haddad`. **Step 5, the automatic reopen:** replying to the seeded `Pending` request returned **`ticketStatus: "Open"`, `statusChanged: true`** with a message payload carrying **neither `channel` nor `authorRole`** (§6.4). **Step 6, feedback:** `201` on a `Resolved` request and `hasFeedback` flips to **`true`** on both the detail and the list row; a second submission → **`409 feedback-already-submitted`**; a rating of `6` and of `0` against a configured `1–5` → **`400` "A rating must be between 1 and 5."** with **no row written**, so the one allowed submission is not consumed; a request that never reached `Resolved` → **`409 feedback-not-available`**; Amina rating Chen's request → **`404`**. **Schema, read off the live database:** `IX_CustomerFeedback_TicketId` is **unique**, the FK is **`NO_ACTION`**, and **`sys.check_constraints` is empty for the table** — a duplicate row inserted directly was refused with error **2601**, while a rating of **`999` was accepted**, which is the proof that **OQ-1 is not encoded in the schema**. **`DateTimeOffset` ordering, which the SQLite test host cannot prove**, holds on real SQL Server in both directions: default is newest-first, `sort=createdAt:asc` reverses it, and `sort=priority:desc` → **`400`** (AP-15) |
| **Story 13 — the four portal screens in a real browser at 390 px** | ✅ Passed 2026-09-01 | Driven against the running `web` and `api` containers on a **fresh seeded volume**, signed in as a seeded portal customer, viewport **390 × 844**. **22 checks, all passing**, screenshots captured. **Shell (§4.2):** exactly **two** nav destinations (`My requests`, `Help`), **no `.layout-sidebar`**, **no notification bell**. **§10.3, single column and no sideways scroll:** on the list, the submit form, the detail and Help, `document.documentElement.scrollWidth - window.innerWidth` is **0 px** on every one, and all cards share **one** x-position. **§7.1:** cards render, and the *"Response needed from you"* cue appears on the `Pending` request; no `Priority`, `Department`, `Assignee` or `SLA` string appears anywhere in the list's text. **§7.2:** the form names no department and no priority (A-14, A-6). **§7.3:** on a `Pending` request **no reopen control and no cancel control are rendered**; posting a reply moves the chip **`Pending` → `Open` in place with the "reopened" cue** and no page reload; on a `Resolved` request the reopen control **is** rendered. **OQ-1:** the rating control rendered **`["1","2","3","4","5"]`, read from `GET /config`'s `feedback.ratingScale` in the same check** — the assertion compares the DOM against the configuration, so a hardcoded widget fails it; an **already-rated** request renders **zero** rating steps and **no nag, reminder or re-prompt anywhere in its text**. **Task 12:** `/portal/help` sits inside the portal shell, and a missing article renders the same not-found state an internal one does. **One defect was found this way and fixed**: the shared `ReplyComposer` was showing its staff placeholder — *"Write a reply to the customer…"* — to the customer; it now takes a `placeholderKey`, and the portal passes its own |
| **Story 13 — slice boundary and regression** | ✅ Passed 2026-09-01 | **No story 14 work:** `TicketInternalNote` does not exist, no `/internal-notes` route exists on either path space, and `InternalNotesAreUnreachableTests` is **still skipped with its `Assert.Fail` body intact** — story 13 asserted the *routing* half in `PortalIsolationTests` and left story 14's entity-level test untouched, as the plan directs. **No story 15 work:** no report, no aggregate, no satisfaction endpoint — only the seeded rating story 15 will read. **No eleventh backend module** (DM-7): `Application/Modules` and `Domain/Modules` still hold ten each, and `CustomerFeedback*` sits under `Tickets`. **No approved document was edited.** **Regression:** the full backend suite — every stories 01–12 test — passes unchanged at **417/1**, and the front-end suite at **66**; the seeded staff surfaces (`/tickets`, `/customers`, `/config/staff`, `/departments`) all still answer for an agent token |

