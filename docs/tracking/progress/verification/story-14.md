[← Back to PROJECT-PROGRESS.md](../../PROJECT-PROGRESS.md)

### Story 14 — `tasks-internal-notes` (2026-09-06)

**Every row below records a command executed in story 14's own verification task.** None is carried
over from an earlier slice. ⚠ **The last row is outstanding, and because of it story 14 is recorded
as Implemented with verification partial — not Verified.**

| Check | Result | Evidence |
|---|---|---|
| Backend build | ✅ Passed 2026-09-06 | `dotnet build backend/SupportCrm.sln` → **Build succeeded, 0 Warning(s), 0 Error(s)**, with `TreatWarningsAsErrors` on |
| Backend suite | ✅ Passed 2026-09-06 | `dotnet test backend/SupportCrm.sln` → **430 passed, 0 failed, 0 skipped**. Delta **+13 passed, −1 skipped** against story 13's 418/1. **The skip is gone because story 14 implemented it**, not because it was deleted |
| Story 14 filtered suite | ✅ Passed 2026-09-06 | `dotnet test backend/SupportCrm.sln --filter "FullyQualifiedName~InternalNotesAreUnreachable\|FullyQualifiedName~TicketTask"` → **13 passed, 0 skipped** — the plan's own step 2, all thirteen it names, including both raw-JSON leak checks |
| Front-end build | ✅ Passed 2026-09-06 | `npm run build` → bundle generation complete; `ticket-detail-component` lazy chunk **80.25 kB** |
| Style lint | ✅ Passed 2026-09-06 | `npm run lint:styles` → exit **0**. Both new regions style **inline**, which stylelint does not scan, so they were additionally grepped for physical properties → **none**; logical properties only |
| Front-end specs | ✅ Passed 2026-09-06 | `npx ng test --watch=false --browsers=ChromeHeadless` → **TOTAL: 66 SUCCESS**, unchanged from story 13 and **with no existing spec edited** |
| Migration — incremental | ✅ Passed 2026-09-06 | `docker compose up -d --build api` against the **existing** dev volume → *"Applying migration '20260906153211_TasksAndInternalNotes'"*, 5 seeders ran, *"Now listening on: http://[::]:8080"* |
| Migration — from scratch, and the seeder (plan task 6) | ✅ Passed 2026-09-06 | A throwaway database `SupportCrmVerify14` on the same server: all **8** migrations applied in order, and `TicketSeeder` (order 40) logged **"11 ticket(s), 7 message(s), 1 rating(s), 1 internal note(s) and 1 task(s) seeded"**. SQL confirmed both rows sit on ticket …403, which carries **3 customer messages** — the one-screen agent-vs-customer contrast task 6 requires. Database dropped afterwards; **`docker compose down -v` was NOT used and the dev volume was never wiped** |
| Canary leak check (plan step 3) | ✅ Passed 2026-09-06 | Posted `INTERNAL-CANARY-9137` as the billing agent (**201**), then `grep -c` over the **raw** responses: portal `/messages` **0**, portal `/{id}` **0**, portal `/tickets` **0**, `/customers/{id}/timeline` **0**, and `"Internal"` in the timeline **0** — measured against a thread of **3** messages, so the zeros are meaningful rather than vacuous. The staff notes read returns it (**1**) |
| Path check (plan step 4) | ✅ Passed 2026-09-06 | `grep -rn "internal-notes" backend/src/SupportCrm.Api/Controllers/` → **`TicketsController` only**. Live: a Customer on the staff route is **403** on GET *and* POST **for their own ticket**; `/portal/.../internal-notes` **404**; `/portal/.../tasks` **404** |
| Task round trip (plan step 5) | ✅ Passed 2026-09-06 | Live `PATCH {isDone:true}` → `completedAt=2026-09-06T15:52:57Z`; `PATCH {isDone:false}` → `completedAt` **absent**. §5 constraint 23 in both directions, through the API |
| AP-10 + row-level re-read | ✅ Passed 2026-09-06 | `PATCH` carrying `completedAt` → **400**; the row **re-read afterwards** → `isDone=False, completedAt=<absent>`. A status code alone would not have proven nothing was written. Cross-department assignee → **422** on the reused `assignee-out-of-department` slug |
| AP-4 by comparison | ✅ Passed 2026-09-06 | Out-of-department vs non-existent ticket, on **both** new route families, asserted **against each other**: identical `type: not-found`, `title`, `status: 404` and `detail: "Ticket not found."` — differing only in the echoed `instance` path and the per-request `traceId` |
| Guard is load-bearing | ✅ Passed 2026-09-06 | Reverted `Visibility = Internal` → `CustomerVisible` in `TicketActivity.InternalNotePosted`; `dotnet test` → **2 failed / 428 passed**, and the two are **exactly** the timeline leak check and the history visibility assertion — nothing legitimate broke. **Line restored**, confirmed by `git diff --stat` reporting **68 insertions, 0 deletions** (no pre-existing line altered); re-run → **430 passed, 0 skipped** |
| Raw-JSON sweep | ✅ Passed 2026-09-06 | Both new payloads searched for `passwordHash`, `storagePath` and `AQAAAA` → all **0**, and for the **actual 84-character hash read out of the `Users` table** → **0**. Searched over the raw response text, against the stored value rather than the property name |
| Schema as created | ✅ Passed 2026-09-06 | `sys.columns` / `sys.indexes` on the live database: `Title nvarchar(200)` (§6.1 `Name`), `Body nvarchar(max)` (`Text`, unindexed), **`CompletedAt` the only nullable column in either table**; both §6 indexes present with the exact declared key order |
| `DateTimeOffset` ordering | ✅ Passed 2026-09-06 | Three tasks created **out of due order** across `+04:00`, `-05:00` and `Z` → returned **soonest-due first by instant** (the `-05:00` row normalising to `04:00Z` sorts first). **SQLite cannot prove this**, which is why it is here |
| Published path space | ✅ Passed 2026-09-06 | `openapi/v1.json` fetched live → **48 paths, 65 operations** = api-design's 66 endpoints **minus story 15's `GET /reports/dashboard`**, which is not built. Story 14's three paths present; **no `/portal` path mentions notes or tasks** |
| Restore after verification | ✅ Passed 2026-09-06 | The canary note, its activity row and the 4 verification tasks were deleted and the result **re-read**: notes **0**, tasks **0**, note-activity **0**. Pre-existing seeded data confirmed intact — 11 tickets, 8 messages, 1 rating |
| **Front-end screen checks (plan step 7; ui-design §10)** | ⚠ **OUTSTANDING — not run** | **No browser-driving capability was available in the verification task**: `require.resolve` reported **puppeteer NOT installed** and **playwright NOT installed**, and installing one would have been a dependency change outside the slice. Karma's ChromeHeadless runs specs, not screens. **Nothing was fabricated and no result is claimed.** Still to be covered: **(1)** the internal-notes region is visually distinct with the **persistent** marker, present when the list is empty and while typing (UI-5); **(2)** the portal request detail shows **no note and no marker**; **(3)** overdue task rows are visually distinct and the add dialog's picker is **Gregorian in Arabic** (A-11); **(4)** RTL **mirrors** rather than re-aligns, and there is **no horizontal body scroll at 390 px** (T3-F). **Until these are covered story 14 is not verified in full and does not count toward §1.1** |

