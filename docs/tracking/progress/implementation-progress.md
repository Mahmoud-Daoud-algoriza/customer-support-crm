[← Back to PROJECT-PROGRESS.md](../PROJECT-PROGRESS.md)

## 7. Implementation Progress

**Stories 01, 02, 03, 04, 05, 06, 07, 12 and 13 are implemented and verified, story 16 in full
with them.** **Story 14 is implemented (plan tasks 1–7, 9, 10) with its verification PARTIAL** — the
backend, data-layer and live-stack checks passed 2026-09-06 and are in §8; **its front-end screen
checks are outstanding** and are listed there as such. **Its plan task 8 is not built (S9-1).** **Story 15 is implemented (all six tasks) with its verification PARTIAL** on the same footing — backend, data layer and live stack verified 2026-09-06, **front-end screen checks outstanding** — but with **no blocked criterion**, because PF-4 was closed first (R-18).
Story 04 is complete across all fourteen plan tasks, **story 05 across all thirteen, story 06 across
all eleven and story 07 across all ten — the last three delivered whole rather than in slices**.
Every claim below was re-verified by running the commands in §8 on **2026-08-31**, not inferred from
a plan — and **story 14's rows were re-verified on 2026-09-06 by commands run in its own verification
task**, with the one check that could not be run recorded as outstanding rather than assumed.

| Area | Status | Evidence |
|---|---|---|
| Backend | ✅ Stories 01, 02, 03, 16 A, **04, 05 and 06 in full**, + the I-9 and I-10 fixes | `dotnet build`: **0 warnings, 0 errors** with `TreatWarningsAsErrors`. **AP-10 is enforced globally** since 2026-08-28 — `UnmappedMemberHandling.Disallow` on the MVC JSON options, so an unmapped request member is a `400` on all six body-binding endpoints (finding I-9, closed) — and **AP-2 now holds for every error path**: `ModelStateProblemDetails` gives the model-state `400` the same `validation-failed` slug and §6.12 envelope the exception path already had, with no .NET internals in the payload (finding I-10, closed). `SupportCrm.Domain` still has **0 project and 0 package references** and no EF attribute (AD-2, AD-4). **Story 05 adds the ticket core**: `Ticket` and `TicketActivity` in the Domain with `SlaClock` beside them (architecture §2.1 puts SLA target calculation there), **`TicketScope` as the single department-scoping helper every later ticket query composes** (AD-5), `TicketService` with auto-assignment by category→department (A-14), the `TicketActivityRecorder`, and `TicketsController`'s seven operations. **A-20 is code, not a comment**: `SlaClock.OnPriorityChanged` is a deliberate no-op that `PatchAsync` still calls, so stories 06 and 09 find one home for the rule. **Ahead of story 06, the A-21 foundation is in**: `Application/Modules/Sla/EscalationRecipientPolicy.cs` behind `IEscalationRecipientPolicy` resolves escalation recipients through one cascade — department manager → every active `Manager` → every active `Administrator` → nobody — logging the fallback at `Warning` in the policy so the manual and automatic escalation paths cannot diverge. **Story 06 is now its first caller.** Story 06 adds the lifecycle half: `TicketLifecycle` holds A-5's graph as data and `Ticket.TransitionTo` is **the only writer of `Status`** in the codebase; `TransitionAuthority` holds A-16 **separately**, so the two rules fail differently (`403` vs `409`); `Escalation.RaiseOneLevel` is the AP-7 action; `TicketLifecycleService` enforces the §5.6 order — scope → authority → legality — in one unit of work with its activity row and audit entry; `TicketActivityQueryService` publishes the history; and `INotificationPublisher` is declared with a **logging** implementation that story 09 swaps for the persistent one. **Story 07 adds the channel seam and the portal path space**: `TicketMessage` in the Domain with `MessageChannel` and `MessageDirection` beside it — **immutable by construction**, no mutator and no setter (§5 constraint 16); `TicketMessageService.PostAsync` is **the one ingestion method** for the staff endpoint, the portal endpoint and story 18’s in-process adapter, and it **does not branch on channel** — the value is a parameter that is stored, which is the whole claim architecture §5.2 makes; `PortalTicketService` publishes `POST /portal/tickets` and **creates nothing itself**, calling the extracted `TicketService.CreateCoreAsync` so the A-14 derivation, the A-3 clock and the `Created` row have one home shared with the staff create; `TicketActivity.MessagePosted` and `TicketActivityRecorder.RecordMessagePostedAsync` make §2.7’s *"set if and only if"* a **signature** rather than a checked rule — no other factory accepts a message id and that one accepts no type; and `Ticket.MarkFirstResponded` holds the "set once" half **on the entity**, so a second outbound message is a no-op rather than a caller remembering to check. **R-13 is not re-implemented**: `PostAsync` calls story 06’s `ApplyAutomaticCustomerReplyTransitionAsync`, which owns the *"only from `Pending`"* guard **Story 12 adds the knowledge base**: `KnowledgeArticle` in the Domain with `ArticleType` and `ArticleVisibility` beside it — **no navigation to `Ticket`, and none may be added** (data-model §2.13), **no versioning and no delete method**, and `Update` that **cannot reach `IsPublished`**, so §6.11’s one-path rule is enforced by the entity rather than only by the request model; `ArticleSearch` is **the single implementation of AD-13** named by 00-implementation-plan §6 — `EF.Functions.Like` over title and body, scored by the weighted count of matched terms with a title match above a body match, built as one SQL expression so the database both filters and orders by it; `PortalArticleService.PortalVisible` is **the single implementation of §5 constraint 19**, composed before the id comparison so an internal article is *not found* rather than found-and-refused (AP-4); and `SuggestedArticleService` scopes through `TicketScope` first, **references no `IAiAssistService`**, and is registered beside the other Knowledge services and deliberately not beside the AI seam (AP-14). `KnowledgeController` (six endpoints, `RequireAgent` to read and `RequireAdministrator` to write), `PortalKnowledgeController` (two) and one added `GET /tickets/{id}/suggested-articles`. **There is no delete endpoint anywhere.** **Story 13 completes the portal path space and adds the sole CSAT input**: `CustomerFeedback` sits under `Domain/Modules/Tickets` — **DM-7**, so no eleventh backend module was created — with **private setters and no mutator at all**, which is what makes §2.15's *"not editable, not resubmittable"* structural rather than a rule a service remembers; **no range constant appears in the Domain, and its factory range-checks nothing** (**OQ-1**). `CustomerFeedbackService` is the one home of the four preconditions and their order is the contract — **scope first**, so a customer never learns from a `409` that another customer's ticket exists; then *has reached `Resolved`*, read from **`ResolvedAt` rather than the current status**, because that stamp is the only record the ticket ever got there (a `Closed` ticket qualifies, and so does one the customer reopened); then one-per-ticket; then the **configured** range, read from `IOptions<FeedbackOptions>` on every call. `PortalTicketService` gained `ListAsync`, `GetAsync` and `TransitionAsync`; the last **delegates to story 06's `TicketLifecycleService`** and restates no part of A-16, then re-reads the ticket as `PortalTicketDto` so the staff DTO the lifecycle service returns **cannot escape through a portal route**; `hasFeedback` is an **`EXISTS` projection over the unique index, never a column** (api-design §7, finding N-4). `PortalTicketsController` publishes the remaining six of §5.7 — list, detail, transition, both attachment actions and feedback — taking the portal to **eleven endpoints exactly**, and story 07's `Location` header was retargeted from the thread to the detail action now that one exists. **There is still no route under `/portal` that reaches an internal note** (T2-C, AP-5), and a test asserts it by routing rather than by response body. **Story 14 adds the two collaboration entities and their five endpoints**: `TicketInternalNote` has **private setters and no mutator**, and — the point of the design — **lives in its own table that no customer-facing query names**, so §2.9's visibility rule is structural rather than a filter (`TicketInternalNoteService` contains no exclusion logic **because there is nothing to exclude**). `TicketTask.SetDone` is the **single writer of `CompletedAt`**, which is §5 constraint 23 expressed as the only assignment path. `TicketActivity.InternalNotePosted` mirrors story 07's `MessagePosted`: it accepts **neither a type nor a visibility**, so §2.7's *"set if and only if"* **and** *"always `Internal`"* cannot be spelled wrongly at a call site — a property proven by reverting the line and watching exactly the two rules' tests fail (§8). `TicketAssigneePolicy` is a **new single home** for §5 constraint 10, extracted from `TicketService` because story 14's plan requires the task assignee to reuse it (deviation **D-1**, §6.8) — the rule is unchanged and story 05's assignment tests pass untouched. `TicketsController` gains five `RequireAgent` actions and **`PortalTicketsController` gains nothing**: there is no portal counterpart and none may be added (AP-5). **`TicketTaskService.ListForUserAsync` exists, is marked, and is called by no controller** — that is **S9-1**, not an oversight. **Story 15 adds the reporting module**: `Application/Modules/Reporting/` with `DashboardReportService` and four query types — `TicketCountsQuery`, `SlaAttainmentQuery`, `AgentPerformanceQuery`, `SatisfactionQuery`. **No entity, no DbSet, no migration** (data-model §7): every metric is an aggregate. The two filters are applied **once**, in `Filtered`, with `branchId` resolved **through the customer** (§4.4) — and **`TicketScope.ForCaller` is deliberately absent**, with the reason written at the code so nobody "fixes" it (AD-5). `AgentPerformanceQuery` holds the **PF-4 reading** (R-18) in one expression. `SlaAttainmentQuery` enumerates **every** priority — the one place in the dashboard that fills in empty groups, because "which priorities are healthy" is unanswerable if a priority can vanish — and returns `null`, never `100`, when there is nothing to measure. `SatisfactionQuery` publishes the configured scale beside the average and **contains no range literal** (OQ-1). `ReportsController` is **one** `RequireManager` action that also enforces AP-15's whitelist (finding I-39). |
| Frontend | ✅ Stories 01, 02, 03, **04, 05 and 06** + error-handling layer | Angular 20.1.2 + PrimeNG 20.0.0 on Sakai `20.0.0`. `npm run build` succeeds; `npm run lint:styles` clean. Sign-in, guards, role redirect, staff shell avatar menu and the admin user screens all exercised in a real browser. A cross-cutting HTTP error-handling layer landed 2026-08-28: `errorInterceptor` applies the cross-cutting half of ui-design §9's status table — `401` ends the session, `403` routes to `/403` without ending it, `5xx` and network failures raise one translated toast, and `400`/`409`/`413`/`422`/`404`/`503` pass through untouched for the feature to render inline. **Story 04 slice 6 adds the first business screens**: typed `CustomersClient` and `AttachmentsClient` (no component calls `HttpClient` — architecture §2.2), the **customer directory** at `/workspace/customers` with `q`, `branchId` and `page` in the URL (**UI-9**), the **four-region customer detail** each with its own loading, empty and error state (§9), a shared **`AttachmentList` + uploader** in `shared/` for the ticket and portal surfaces to reuse, and the **registration form** Story 02 scaffolded, now enabled. All driven through a real browser against the running `web` container — 18 UI checks and 3 phone-width checks, §8. **Story 05 adds the workspace ticket surface**: a typed `TicketsClient`, the ticket list at `/workspace/tickets` with a URL-borne filter bar (**UI-9**), the ticket detail header, the `Assign` control and the customer panel, plus shared `StatusChip`, `PriorityChip` and `TicketFilterBar`. Two front-end findings came out of driving it — **I-18** (`NG0950` from reading a required input in a constructor) and **I-19** (a deep link losing its filters on load). **Story 06 adds the lifecycle controls**: a `TransitionMenu` offering **legal ∧ permitted** (UI-3), a separate `Escalate` control with a UI-12 confirmation that now **states the notification** (A-21 lifted the ui-design §11 constraint), the **activity region** rendering actor / timestamp / before-and-after, and a populated customer timeline. **No optimistic UI** (UI-8) — the chip moves only after the server confirms, and a refusal renders from the problem `type`. The A-5 and A-16 client copies live in **one** file, `shared/lifecycle/transition-matrix.ts`, labelled **F-1**, and are pinned by 9 specs. **Story 07 adds the thread**: `MessageThread` is **two configurations of one component** (ui-design §8) — the staff one shows `channel` and `authorRole`, the portal one does not, and the flag **defaults to the narrower rendering** so a forgetful caller leaks nothing; `ReplyComposer` is **one component with one insertion point** (UI-7), which is what will keep A-8’s *"never auto-sent"* true when story 08 adds quick replies and story 11 the AI draft into the same draft. Inbound and outbound sit on opposite sides using **`margin-inline-*` and `border-inline-*` only**, so Arabic mirrors them with no second rule — `lint:styles` bans the physical alternatives. A typed `PortalClient` is **separate from `TicketsClient` and shares no DTO type with it** (AP-5), and it has **no internal-notes method** — not "not yet", but never. **Nothing polls**: no interval, no socket, no subscription, and no wording in the UI, the code or the README calls any of it real-time chat (T3-B). The portal `requests/new` and `requests/:id` routes are **deliberate stubs story 13 replaces** **Story 12 adds four surfaces and one shared reader**: `/workspace/knowledge` with URL-borne `q`/`type`/`visibility`/`isPublished` (**UI-9**) and an **internal badge** on internal rows; `/workspace/knowledge/:id`; the **suggested-articles region** below the AI panel in the ticket detail — a separate component with its own wording (*"Existing articles found by keyword in this ticket"*), **no sparkle icon, no AI-generated label and no dismiss**, because it sits next to a panel that has all three and must not be mistaken for it (AP-14); `/admin/knowledge`, `/admin/knowledge/new` and `/admin/knowledge/:id` with a **plain textarea plus a live preview, no toolbar, no media library, no upload, no delete and no version history**, and **Publish / Unpublish as explicit buttons separate from Save** mirroring the API’s action pair; and `/portal/help` with `/portal/help/:id`, whose `404` renders the **same wording** for missing, internal and unpublished (AP-4, §9). `MarkdownViewComponent` parses basic markdown into blocks and spans and renders them **through the template — never `innerHTML`**, so authored content reaching a customer screen is text rather than markup; **no body is passed through a translation pipe** (A-11). Typed `KnowledgeClient` and a **separate** `PortalKnowledgeClient` sharing no DTO (AP-5). **Story 13 replaces the Story 07 stubs with the four portal screens of §7 and the shell of §4.2**: `PortalShellComponent` no longer wraps the staff `AppLayout` — it renders **its own header with two destinations, the language switcher and the avatar menu, and has no sidebar and no notification bell**, and the Customer branch was removed from `app.menu.ts` because a customer never mounts it. `/portal/requests` is a **card list**, single column at every width, with the *"response needed from you"* cue on `Pending` and a status filter bound to the URL (**UI-9**); `/portal/requests/new` keeps its **exactly four inputs**; `/portal/requests/:id` is the five-region detail — header, thread, composer, attachments, feedback — where **cancel renders only while `New`** and is replaced by a line once work has started, **reopen renders only on `Resolved`**, **no manual reopen is ever offered on `Pending`** (the client's `PortalTransitionTarget` admits only `'Cancelled' | 'Open'`, so the mistake is unspellable), and the reply updates the chip **from the response envelope, not a re-fetch** (R-13). `Closed` and `Cancelled` requests are **read-only with a line saying so**, and the composer renders that sentence instead of a box. **`shared/components/rating-input/` takes `{ min, max }` and renders an ordinal scale** — **no star widget, no `1..5` array, no thumbs pair** (ui-design §11) — with `binaryLabels()` left as a **documented, deliberately inert seam** should OQ-1 decide binary. `ReplyComposer` gained a **third configuration point**, `placeholderKey`, because its staff default reads *"Write a reply to the customer…"* and the portal's reader **is** the customer. **No file under `features/portal/**` imports `PriorityChip`, `SlaIndicator` or the customer panel.** **Story 14 adds the two ticket-detail regions of ui-design §5.3.** The **internal-notes region** sits between the thread and the activity region, is a distinct amber block, and carries the *"Not visible to the customer"* marker **in its header, always rendered** — not a hover state, not a per-item badge, and **not conditional on the list being non-empty**, because an empty region is exactly when someone is about to write one (**UI-5**). It reads **its own endpoint**: there is no merged list and no client-side filter anywhere on the screen, so a rendering bug cannot leak a note into the conversation above it. It offers **no edit and no delete control**, because neither exists server-side. The **tasks region** sits in the side column below the suggested articles, renders title / due date / assignee / done checkbox with **overdue rows distinguished by colour *and* a text label**, and adds tasks through a small dialog whose date picker is Gregorian in both languages with **no calendar view, no recurrence and no reminder control** (T2-C, A-11). Its assignee picker **reuses I-16's recorded resolution** rather than inventing an endpoint (§6.8). Both regions style with **logical properties only**. ⚠ **These regions have not been driven in a browser** — see §8's outstanding row. **Story 15 adds the management dashboard**: `ReportsClient`, the `/workspace/reports` route behind a **`Manager+`** guard, the Reports nav entry for Manager and above, and the four regions of ui-design §5.7 — each with **its own empty state**. **"Empty is not zero" is rendered three ways**: a null attainment percent and a null average resolution both render **"—"**, and zero responses renders *"No ratings submitted yet"* rather than "0.0". The satisfaction tile renders the average **beside its configured scale**, hardcoding no denominator (OQ-1). **"Tickets assigned" carries no clarifying tooltip** (ui-design §11). Filters are **URL-bound** (UI-9) and the tables **scroll inside their own container** so the body never scrolls sideways (architecture §2.3). **No charting dependency was added** — PrimeNG table and tag only. ⚠ **The dashboard has not been driven in a browser either** — same §8 outstanding row. |
| Database | ✅ Schema live | **`InitialSchema` and `Customers` both applied to real SQL Server**: `Branches`, `Departments`, `Users`, `AuditEntries`, **`Customers`, `CustomerNotes`, `Attachments`**. `Users.Email` **and `Customers.Email`** both carry `SQL_Latin1_General_CP1_CI_AS` at `nvarchar(256)` — the same address, the same width, the same collation, as §6.1 requires. `Users(CustomerId)` is a filtered unique index and **now has its foreign key to `Customers`**, closing the DM-1 link story 02 left open. `CK_Attachments_OwnerXor` is present and **proven to refuse both a no-owner and a both-owners row** (§5 constraint 20). **Story 05's `Tickets` migration is applied**: `Tickets` and `TicketActivities` exist, and `dbo.Tickets` has **19 columns with no branch column** — a ticket's branch is derived `Ticket → Customer → Branch` (data-model §2.3), verified against the live schema on 2026-08-31. The table carries the story's **filtered indexes**, whose predicates are written as two `<>` comparisons because a filtered-index predicate does not admit `NOT IN` on SQL Server (finding **I-17**, found only by applying the migration to the real database). **Story 06 added no migration and needed none** — the lifecycle writes columns `Tickets` and `TicketActivities` already carry (`Status`, `ResolvedAt`, `ClosedAt`, and the activity rows), which is why `dotnet ef` was not run. **Story 07’s `TicketMessages` migration is applied**, read back off the live schema on **2026-08-31**: seven columns with `Direction` and `Channel` at **`nvarchar(64)`** (the §6.1 `Code` tier), `Body` at **`nvarchar(max)`** (the `Text` tier, **never indexed**) and `PostedAt` as `datetimeoffset`; the **`(TicketId, PostedAt)`** index §6 declares for thread rendering; and **`FK_TicketActivities_TicketMessages_MessageId`**, which completes the `TicketActivity.MessageId` relationship story 05 mapped as a bare column. `dotnet ef migrations has-pending-model-changes` reports **no model change**. The FK is `NO_ACTION` on purpose — §5 constraint 17 says every message has exactly one activity row, so a message must not be removable while its row points at it **Story 12’s `KnowledgeArticles` migration is applied to real SQL Server** (2026-09-01): `Title` at `nvarchar(200)` (the §6.1 `Name` tier), `Body` at `nvarchar(max)` (the `Text` tier, **never indexed**), `Type` and `Visibility` at `nvarchar(64)` (the `Code` tier, stored as string codes), both timestamps `datetimeoffset`, and `FK_KnowledgeArticles_Users_AuthorUserId` as `Restrict`. **No full-text catalogue and no search index were created** — data-model §6 states a contains-match over the two columns needs none at these volumes, and records full-text as the available upgrade rather than this story’s work. **There is no foreign key to `Tickets`** (§2.13). **Story 13's `CustomerFeedback` migration is applied to real SQL Server** (2026-09-01), read back off the live schema: five columns — `Rating` as `int`, `Comment` as `nvarchar(max)` (the §6.1 `Text` tier, **never indexed**), `SubmittedAt` as `datetimeoffset` — the **unique** `IX_CustomerFeedback_TicketId` that §6 declares and §5 constraint 21 requires, and `FK_CustomerFeedback_Tickets_TicketId` as **`NO_ACTION`** rather than cascade: a rating is a reporting fact feeding the §9.4 average, and the *absence* of a row is meaningful, so deleting one with its ticket would silently change a reported number. **There is deliberately NO check constraint on `Rating`** — proven on the live database, which accepted a rating of `999` inserted directly — because a range here would encode **OQ-1** into the schema, which data-model §2.15 forbids. **Uniqueness is enforced by the database, not only by the service**: a duplicate row inserted directly was refused with SQL Server error **2601**. **Story 14's `TasksAndInternalNotes` migration is applied to real SQL Server** (2026-09-06), **twice and two ways**: incrementally onto the existing dev volume, and **from scratch** onto a throwaway database created for the check and dropped afterwards — **the dev volume was never wiped**. Read back off the live schema: `TicketInternalNotes` with `Body` at `nvarchar(max)` (the §6.1 `Text` tier, **never indexed**) and `CreatedAt` as `datetimeoffset`; `TicketTasks` with `Title` at `nvarchar(200)` (the `Name` tier), both timestamps `datetimeoffset`, and **`CompletedAt` as the only nullable column in either table** — the nullable half of §5 constraint 23. Both §6 indexes exist with the exact declared key order: **`TicketInternalNotes(TicketId, CreatedAt)`** and **`TicketTasks(AssignedUserId, IsDone, DueAt)`**, the latter created **regardless of S9-1** because it is the data model's requirement, not the endpoint's. **`FK_TicketActivities_TicketInternalNotes_InternalNoteId`** completes the `TicketActivity.InternalNoteId` relationship story 05 mapped as a bare column — `Restrict`, matching the `MessageId` FK and for the same §5 constraint 17 reason. Row-level checks on the from-scratch database: **0** rows violating constraint 23, and **1 note → 1 `InternalNotePosted` activity row, 0 at the wrong visibility, 0 unlinked** (constraint 17, §2.7). |
| Configuration | ✅ Validated at startup | Seven option types bound and `ValidateOnStart`. Six checks: every category maps to an existing department (A-14), every priority has an SLA target with positive hours (A-3), `DefaultBranchId` is an existing branch (A-15), `Priorities` equals the A-6 levels in order, `Min < Max` on the rating scale (structural only — **OQ-1 stays open**), and a positive attachment cap. **All six proven to stop the host** by starting the real API with each value broken in turn |
| Seed data | ✅ Running at startup | 2 departments (both with a manager), 2 branches, 4 staff users — Administrator, Manager and **two Agents in different departments**, so Story 05's scoping tests have material. **Story 04 slice 3 adds `CustomerSeeder` (`Order = 30`)**: 4 customers spread across **both** branches (which is what makes Story 05's "branch is not a boundary" test meaningful), **2 with a portal login and 2 deliberately without** — both DM-1 shapes, so A-19 is demonstrable by hand — plus one note and one attachment. A seeded portal login signs in successfully. Password from configuration; no credential in source. **Story 06 added no seeder and changed no seeded row** — the lifecycle is exercised against the tickets story 05 seeds. **Story 05 adds `TicketSeeder`**: **6 tickets across both departments and a spread of priorities and statuses**, which is what makes the list filters and the department-scoping checks demonstrable by hand — a Billing agent sees **3**, all in their own department. **Story 07 extends it with 4 messages and two lifecycles**, both driven through `Ticket.TransitionTo` so A-5 refuses an illegal edge **from a seeder too**: the payments ticket carries a **two-way thread** (agent → customer → agent) and the billing ticket is **left in `Pending`** with the customer who owns it holding a seeded portal login — so **the R-13 reopen is one click away in the demo**. Re-running against an existing volume seeds nothing, thread included. Finding **I-26** came out of this row **Story 12 adds `KnowledgeSeeder` (`Order = 50`)**: **10 articles across all three types and both visibilities**, with bodies whose words overlap the seeded tickets’ subjects so the suggested-articles region returns non-trivial results in the demo — the refund ticket retrieves five, top-scored at 12. **One of the ten is public and deliberately unpublished**, so a demo can show that visibility and publication are separate facts rather than one. Re-running against an existing volume seeds nothing. **Story 13 extends `TicketSeeder` with three `Resolved` requests and one rating**, so every branch of §7.3 is demonstrable in one sign-in: **one `Resolved` request with no feedback for each of the two portal customers**, so the rating control actually appears whichever login is used; **one that is already rated**, so Story 15's §9.4 tile has a real average and the "already rated, no control" branch is visible; and the `New` and `Pending` rows already present carry cancel (A-16, A-18) and the automatic reopen (R-13). Both new transitions go through `Ticket.TransitionTo`, so `ResolvedAt` is stamped by the entity exactly as an endpoint stamps it — which matters, because that is the field the feedback precondition reads. **The seeded rating is read from `FeedbackOptions.Max`, never written as a literal** (**OQ-1**): a hardcoded number in a seeder is the quietest place of all to encode a scale. Startup reports `11 ticket(s), 7 message(s) and 1 rating(s) seeded`. |
| Tests | ✅ **451 passing, 0 skipped**, plus **66 front-end specs** | Backend: `AuthorizationTests`, `UserAdminValidationTests`, `AuditRecordingTests`, `PlatformEndpointTests`, `OrganizationEndpointTests`, and story 16 Part A's `ConfigurationTierTests` (8), `ConfigurationValidationTests` (10) and `NoConfigurationEntityTests` (18). Includes the **AD-15 deactivation regression** and the indistinguishability of a wrong password from a deactivated account. `BranchIsNotABoundaryTests` **was skipped until story 05** created `Ticket`; **it now runs**, and the suite has **no skips left**. Story 04 slice 1 adds `CustomerDataLayerTests` (14) and `AttachmentStorageTests` (8); slice 2 adds `CustomerServiceTests` (24), which walks the **whole A-19 case table**; slice 3 adds `CustomerNotesAndTimelineTests` (9), `AttachmentServiceTests` (11) and `CustomerSeederTests` (6); slice 4 adds `CustomerAccessTests` (15), the **first endpoint suite** for this story — **87 tests** in all. The cross-cutting **I-9 fix adds `UnmappedRequestMemberTests` (16)**, which covers **all six** JSON-body endpoints for AP-10 and pins the three behaviours the fix deliberately left alone (query-string binding, case-insensitive matching, empty `PATCH` bodies). The **I-10 fix adds `ModelStateProblemDetailsTests` (27)**, which walks **seven** distinct producers of a model-state `400` — data annotations, four JSON-reader failure modes, query binding, and the `ValidationException` path they must agree with — asserting the whole §6.12 envelope, camelCase keys, and a **raw-body scan** proving no `SupportCrm.`, `System.`, generic arity, parser internal or RFC URL survives. Slice 5 adds `RegistrationTests` (22), organized around A-15's three outcomes and asserting each at the **row** level — a `201` that quietly wrote a second customer would pass a status check and break A-10. Slice 6 adds **no backend test** — it changed no backend file, and the suite's staying at exactly 220 is itself the evidence for that. Front end: `department-filter.component.spec.ts` (4), the cross-cutting `error.interceptor.spec.ts` (12), and slice 6's `customers.client.spec.ts` (**7**, new) — **23 specs**, on the karma target story 01 already configured. The slice-6 specs are deliberately about **the wire**, which is where a typed client can be wrong in a way no screen reveals until it reaches a server: the list filters carry the API's own names, a null filter is **omitted** rather than sent empty, a `PATCH` body carries only what it was given, the multipart part is named `file`, the download URL is built from the **id**, and the download goes through `HttpClient` so the bearer token is actually sent (**I-13**). **Story 05 adds 31 backend tests and no front-end spec** — `TicketCreationTests`, `TicketScopingTests`, `SlaClockTests` and the newly-unskipped `BranchIsNotABoundaryTests`, taking the suite from **220/0/1 to 251/0/0**. Two of them are **not named by the plan** and were added deliberately (finding **I-15**): A-20's implementation is an empty method body, and an empty method with no test is the one thing a later contributor deletes believing it dead, so `Low → Urgent` — the largest possible tightening — is asserted to move neither due timestamp at both the unit and the endpoint level. **The A-21 policy adds `EscalationRecipientPolicyTests` (8)**, covering every rung of the cascade including the three the seeded demo cannot reach. **Story 06 adds 50**: `TicketLifecycleTests` (18) walks the **full 6×6 legality matrix and its complement** twice — once through `TicketLifecycle` and once through the guarded mutator — `TransitionAuthorityTests` (20) separates A-16 from A-5 and pins the `409`'s `allowedTransitions` and `detail`, and `TicketHistoryTests` (12) proves append-only **by reflection and by a source scan**, the timeline exclusions, the AD-10 independence and R-14's attribution. Front end: **32 specs** (was 23) — story 06's 9 are `transition-matrix.spec.ts`, which pins the **F-1** duplicated matrix against A-5 and A-16 transcribed from product-scope, so the client copy cannot drift from the server's unnoticed. **Story 07 adds 18 backend tests and no front-end spec**, taking the suite from **309/0/0 to 327/0/1**: `MessagingTests` (18) covers the plan's thirteen named cases plus five the plan does not name — the three AP-10 server-derived members as a `[Theory]`, both terminal statuses as another, and **the other half of A-13's rule, that an *unassigned* ticket raises no notification at all**, which is the half an implementation drifts on. Notifications are asserted through a `RecordingNotificationPublisher` swapped in by a new `ServiceOverrides` hook on the test factory, because A-13 has no table until story 09 and **a log line is not assertable**. `InternalNotesAreUnreachableTests` is the **one skip, and it is deliberate**: the plan asks for a named home for the portal-thread isolation check, and its body `Assert.Fail`s so **story 14 must implement it rather than delete it**. No front-end spec: story 07 added no client-side rule of the kind the F-1 and wire specs exist to pin **Story 12 adds 16 backend tests and 11 front-end specs**, taking the suite from **385/1 to 401 passing, 1 skipped** and the karma target from **51 to 62**. `KnowledgeVisibilityTests` (8) covers the plan’s tests 1–7 plus one the plan does not name — **that no delete route exists on either path space**, asserted rather than assumed, because the absence *is* the contract (T2-E). Its AP-4 case compares the **body** of an internal-article `404` with the body of a missing-id `404` and requires them equal, so a future distinguishing message fails a test rather than a review. `SearchAndSuggestionTests` (8) covers tests 8–12 plus three more: the AP-15 sort refusal, an unknown `type` filter, and a search proving matching runs **against the database alone**. Test 12 is asserted twice — the `/ai/suggested-solutions` path is not routable, **and** no registered endpoint’s route template mentions `suggested-solutions`, which is what catches the route being added under another verb or prefix. Front end: `knowledge.client.spec.ts` (6) is about **the wire** — the four filter names, a null filter omitted, `isPublished` absent from both the create and the patch body, the action pair, and **a Knowledge path rather than an `/ai` one** for suggestions — and `markdown-view.component.spec.ts` (5) pins the parser, including that HTML in an article body stays **text**, which is the property that lets the reader avoid `innerHTML` on a customer-facing screen. **Story 13 adds 16 backend tests and 4 front-end specs**, taking the suite from **401/1 to 418 passing, 1 skipped** and the karma target from **62 to 66**. The sixteen are **exactly the sixteen the plan names**, in the three files it names: `PortalIsolationTests` (6) — a customer sees only their own, another customer's id is `404` **and indistinguishable from a missing one** (same `type`, same `detail`), all six other portal actions are `404` on another customer's ticket **enumerated rather than sampled**, a customer on a staff route is `403`, `/portal/tickets/{id}/internal-notes` **is not routable**, and **the serialized JSON of the detail, a list row and the transition response carries none of `assignee`, `departmentId`, `priority`, `firstResponseDueAt`, `resolutionDueAt`, `firstResponseBreached`, `resolutionBreached`** — asserted on the **raw payload, not the DTO type**, because a type is what the server promises and the payload is what a customer receives; `PortalLifecycleTests` (5), which **closes I-23** by asserting A-16's customer column over HTTP for the first time, including cancelling a ticket that **already has an assignee** (A-18 — the test reads the assignee back before cancelling, so it would fail under an implementation that treated assignment as "work has started"); and `FeedbackTests` (5). **Not one test in `FeedbackTests` contains a number that could be a scale** (**OQ-1**): both out-of-range values are computed as `Scale.Min - 1` and `Scale.Max + 1` from the running host's `IOptions<FeedbackOptions>`, so the file survives whatever OQ-1 decides, **including a binary answer**. The write-once test sends a *different* rating on the second call and asserts the first row is unchanged, so a service that updated instead of refusing would fail it. Front end: `rating-input.component.spec.ts` (4) renders `{min:2,max:4}` — deliberately **not** the configured placeholder — and asserts exactly three steps, so replacing the control with five stars fails a test rather than a review; it also pins the two-value case, the no-default-selection rule (declining is normal) and that an impossible range renders **no** scale rather than a guessed one. **Story 14 adds 13 backend tests and no front-end spec**, taking the suite from **418 passing / 1 skipped to 430 passing / 0 skipped**. **The skip count reaching zero is the headline**: `InternalNotesAreUnreachableTests` was story 07's deliberately skipped stub with an `Assert.Fail` body and the instruction *"Story 14 unskips it and fills the body — it must not be made vacuous instead"*, and **story 14 implemented it rather than deleting or hollowing it out**. Its 8 tests are the plan's: all three internal roles read the notes; a Customer is `403` on **both verbs of their own ticket**, so the refusal is unambiguously about the path space; `/portal/.../internal-notes` is **not routable**; and the two leak checks assert on the **raw JSON** using a canary token rather than on a parsed field, against a **populated** thread so the absence means something. Test 8 asserts immutability **by reflection over two types** — the service exposes no `Update`/`Delete`/`Edit`, and the entity exposes **no public instance method at all**. `TicketTaskTests` (5) covers the plan's tests 9–13, including **constraint 23 in both directions** asserted at the row as well as the payload, and the AP-10 refusal **re-read at the row** to prove nothing was half-written. One pre-existing test was **re-expressed, not deleted** — `CustomerNotesAndTimelineTests`, deviation **D-2** in §6.8 — because its own remarks required exactly that once an internal-note set appeared. **No front-end spec was added**: story 14's client additions are wire-shaped like story 12's, but its two regions' behaviour is screen behaviour, and **that is precisely the evidence still outstanding** (§8). **Story 15 adds 21 backend tests and no front-end spec**, taking the suite from **430 to 451 passing, 0 skipped**. `DashboardAccessTests` (9) covers the plan's tests 1, 2 and 9 plus six the plan does not name — anonymous is `401` not `403`, the two approved filters are accepted so the AP-15 guard does not over-refuse, three other reporting routes do not exist, and the endpoint takes no write verb. `DashboardMetricsTests` (7) covers tests 3–8 plus the **PF-4 pin**, whose distinguishing case is a **closed** ticket still stamped with the agent's id: it must not count as assigned. Its rating assertions **derive both values from the configured scale**, never literals, so the file survives whatever OQ-1 decides — the rule story 13's `FeedbackTests` set. `AggregationHappensOnTheServerTests` (2) covers test 10 **by comparison**: the payload for 3 tickets and for 33 must not differ in size class, which catches a row dump that every metric test would pass. |
| Docker / infrastructure | ✅ Complete for the stack | `docker compose up --build` brings **db, api, web** up; the API waits for the database's health check. Story 04 slice 1 adds a **second named volume**, `supportcrm-attachments`, mounted at `/var/lib/supportcrm/attachments` — attachment bytes cannot live in the image (T2-A). **Slice 3 fixed a real defect in that mount** (finding **I-8**): the image runs as a non-root user, the volume was created root-owned, and the first startup that actually wrote a file failed with `Access to the path … is denied`. The directory is now created and `chown`ed in the Dockerfile **before** the `USER` switch, so Docker initializes the volume with the right ownership |

**Endpoints that exist (13):** `/health`, `/config/bootstrap`, **`/config`**, **`/config/staff`**,
`/auth/login`, `/auth/me`, the five `/users` routes, and `GET /departments` and `GET /branches`. `POST /auth/register` is **deferred to
Story 04** by S9-7. **Neither organization route has a write verb**, and neither ever will: T2-I
makes departments and branches seeded configuration.

**Endpoints that exist (24):** the thirteen from stories 01–03 and 16 Part A, plus story 04 slice
4's **ten** — `GET`/`POST /customers`, `GET`/`PATCH /customers/{id}`, `GET /customers/{id}/timeline`,
`GET`/`POST /customers/{id}/notes`, `GET`/`POST /customers/{id}/attachments`, and
`GET /attachments/{attachmentId}/content` — plus slice 5's **`POST /auth/register`** (anonymous).
`openapi/v1.json` publishes **18 paths** (was 17, and 11 before story 04).
**`POST /auth/register` closes S9-7**, which deferred it out of Story 02 because A-15 needs a
`Customer`, a `Branch` and a configured default branch, and none of the three existed then.

**Story 04 slice 6 added no endpoint, and the path count proved it** — **18**, measured on
2026-08-30 against the running API. A front-end slice must add none, and that one added none.

**Endpoints that exist (31):** the twenty-four above plus **story 05's seven** —
`GET`/`POST /tickets`, `GET`/`PATCH /tickets/{id}`, `POST /tickets/{id}/assignment`, and
`GET`/`POST /tickets/{id}/attachments`. `openapi/v1.json` publishes **22 paths** (was 18 after story
04, 11 before it), re-measured on **2026-08-31** against the running API.
**The two ticket-attachment routes close S9-2**, which deferred the ticket half of story 04's
attachment acceptance criterion into this story because no `Ticket` existed to own a file.

**Endpoints that exist (34):** the thirty-one above plus **story 06's three** —
`POST /tickets/{id}/transition`, `POST /tickets/{id}/escalate` and `GET /tickets/{id}/activity`.
`openapi/v1.json` publishes **25 paths** (was 22 after story 05), re-measured on **2026-08-31**
against the running API. **`status` is still not a field on `PATCH /tickets/{id}`** (AP-1) and no
delete verb exists anywhere: one `/transition` endpoint holds the whole A-5 × A-16 matrix (AP-6), and
escalation is its **own** endpoint because it is an action rather than a status change (AP-7).

**Endpoints that exist (37):** the thirty-four above plus **story 07's three** —
`GET`/`POST /tickets/{id}/messages` and, in the **separate portal path space** (AP-5),
`POST /portal/tickets` with `GET`/`POST /portal/tickets/{id}/messages`.
`openapi/v1.json` publishes **28 paths** (was 25 after story 06), re-measured on **2026-08-31**
against the running API. **`grep -rn "channels/inbound\|webhook" backend/src/` returns nothing and
the published path list contains no inbound route** — **AP-11**, which is how **PF-2** is avoided
rather than papered over: story 18's adapter calls `TicketMessageService.PostAsync` **in-process**.
**`direction` and `channel` appear in no request model**, so a body carrying either is a `400`
(AP-10, §7).

**Endpoints that exist — the portal path space is complete at eleven.** Story 13 adds **six**:
`GET /portal/tickets`, `GET /portal/tickets/{id}`, `POST /portal/tickets/{id}/transition`,
`GET`/`POST /portal/tickets/{id}/attachments` and `POST /portal/tickets/{id}/feedback`. With story
07's three (`POST /portal/tickets`, `GET`/`POST /portal/tickets/{id}/messages`) and story 12's two
(`GET /portal/kb/articles`, `GET /portal/kb/articles/{id}`) that is **eleven, matching
[api-design.md](api-design.md) §5.7 endpoint for endpoint** — and **no twelfth is expected**.
**There is no portal download route**, deliberately: `GET /attachments/{id}/content` serves every
role, which is **AP-19**'s single named exception to AP-5's path split, because a byte stream has no
DTO to vary by audience. **`POST`/`PATCH`/`DELETE` do not exist on the feedback resource** — write-once
means the API offers no way to change or withdraw a rating (data-model §2.15).

**Next (as recorded before this task):** **story 08** (`agent-dashboard`) — **blocked on explicit
approval**, and carrying **S9-1** against its task region only. Story 07 delivered the
`ReplyComposer` with **one insertion point**, which is where story 08's quick replies land. Story 16
**Part B** was **unblocked** — story 06 writes the `TicketStatusChanged` and `TicketEscalated` audit
rows its log needs — and stayed at Phase 7 by execution order.

**Story 16 Part B — audit and configuration, complete 2026-09-01.** `AuditQueryService`
(`Application/Modules/Administration`) is the single read method over `AuditEntry`: newest-first,
filtered by `actorUserId`, `action` and a `from`/`to` range, projecting `actor` as `UserSummaryDto?`
exactly as `TicketActivityQueryService` already does for its own actor column. `AuditController`
publishes **one route**, `GET /audit`, gated by the same `RequireAdministrator` policy `UsersController`
uses, with **no write, update, patch or delete action** — confirmed live, `POST`/`PATCH`/`PUT`/`DELETE`
all `405`. **Every one of the ten actions `AuditAction` names already had a live call site before this
task** (Story 02's sign-in and user-administration writes, Story 04's `UserEmailChanged`, Story 06's
`TicketStatusChanged` and `TicketEscalated`) — Part B added **no write path**, only the read surface,
exactly as the plan's task 6 table predicted. The front end adds `AuditClient`, `/admin/audit` (filters
bound to the URL per **UI-9**, a `p-paginator`, **zero row actions**) and `/admin/configuration`
(**zero** `input`/`select`/`textarea`/`button[type=submit]` elements, verified live with the plan's
own DOM query) — the latter reuses the **existing** `PlatformApiService` for `GET /config` and
`GET /config/staff` and the **existing** `RuntimeConfigService` for branding, so **no new HTTP call
was added for a value the application already fetches once at bootstrap**. **This is the last
administrative surface** the plan names — nothing else is consumed by it, and Story 16 now counts as
implemented and verified in full (§1.1).

