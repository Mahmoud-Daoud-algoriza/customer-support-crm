version: 1
project:
  name: Customer Support CRM
  primaryLanguage: C# (ASP.NET Core) + TypeScript (Angular)
  projectRoots:
    - backend
    - frontend
tracker:
  type: none
naming:
  includeTrackerId: false
  globalSequence: true
agents:
  - claude-code
